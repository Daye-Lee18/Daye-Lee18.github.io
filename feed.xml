<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://daye-lee18.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://daye-lee18.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-05-10T01:38:10+00:00</updated><id>https://daye-lee18.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design.
</subtitle><entry><title type="html">Flow Matching (FM) 요약</title><link href="https://daye-lee18.github.io/blog/2026/flow-matching/" rel="alternate" type="text/html" title="Flow Matching (FM) 요약" /><published>2026-05-10T00:00:00+00:00</published><updated>2026-05-10T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2026/flow-matching</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2026/flow-matching/"><![CDATA[<h2 id="1-flow-matching-fm-핵심-개념">1. Flow Matching (FM) 핵심 개념</h2>

<p>Flow Matching은 데이터를 생성하는 확률 경로(Probability Path)를 정의하고, 이를 유도하는 <strong>벡터장(Vector Field)</strong>을 직접 학습하는 생성 모델입니다. Diffusion 모델과 유사하지만, 노이즈에서 데이터로 가는 경로를 더 직접적이고 효율적으로 설계할 수 있습니다.</p>

<h3 id="핵심-구성-요소">핵심 구성 요소</h3>
<ul>
  <li><strong>벡터장 $v_t(x)$</strong>: 시간 $t$에 따라 샘플 $x$가 이동해야 할 방향과 속도를 정의합니다.</li>
  <li><strong>흐름(Flow) $\psi_t(x)$</strong>: 벡터장에 의해 정의되는 궤적으로, $\frac{d}{dt}\psi_t(x) = v_t(\psi_t(x), t)$를 만족합니다.</li>
  <li><strong>확률 경로 $p_t(x)$</strong>: $p_0$(노이즈 분포)에서 $p_1$(데이터 분포)로 변하는 연속적인 분포의 집합입니다.</li>
</ul>

<hr />

<h2 id="2-수학적-구조-및-학습-방법">2. 수학적 구조 및 학습 방법</h2>

<h3 id="conditional-flow-matching-cfm">Conditional Flow Matching (CFM)</h3>
<p>실제 데이터 분포의 벡터장을 직접 구하는 것은 어렵기 때문에, 개별 데이터 포인트 $x_1$에 조건화된 <strong>조건부 벡터장</strong>을 학습합니다.</p>

<ul>
  <li><strong>선형 보간 경로 (Linear Interpolation)</strong>: 가장 흔히 쓰이는 경로 설정입니다.
\(x_t = (1-t)x_0 + t x_1\)</li>
  <li><strong>조건부 벡터장 $u_t(x \mid x_1)$</strong>: 위 경로를 미분하면 얻어지는 목표 벡터장입니다.
\(u_t(x_t \mid x_1) = \frac{d}{dt}x_t = x_1 - x_0\)</li>
</ul>

<h3 id="목적-함수-loss-function">목적 함수 (Loss Function)</h3>
<p>신경망 $v_\theta(x, t)$가 목표 벡터장 $u_t$를 모사하도록 학습합니다.
\(\mathcal{L}_{CFM}(\theta) = \mathbb{E}_{t, x_0, x_1} \left[ \| v_\theta(x_t, t) - (x_1 - x_0) \|^2 \right]\)</p>

<blockquote>
  <p><strong>Tip:</strong> Diffusion 모델과 달리 학습 시 ODE Solver를 거칠 필요가 없으며, 목적 함수가 단순한 MSE 형태이므로 학습이 매우 안정적입니다.</p>
</blockquote>

<hr />

<h2 id="3-코드-응용-implementation-logic">3. 코드 응용 (Implementation Logic)</h2>

<p>PyTorch 스타일의 핵심 로직입니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>

<span class="k">def</span> <span class="nf">flow_matching_loss</span><span class="p">(</span><span class="n">v_net</span><span class="p">,</span> <span class="n">x_1</span><span class="p">):</span>
    <span class="c1"># 1. 시간 t 샘플링 (0 ~ 1)
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">x_1</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">1</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">x_1</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    
    <span class="c1"># 2. 베이스 분포(노이즈) x_0 생성
</span>    <span class="n">x_0</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x_1</span><span class="p">)</span>
    
    <span class="c1"># 3. 선형 보간을 통한 x_t 생성
</span>    <span class="n">x_t</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="n">x_0</span> <span class="o">+</span> <span class="n">t</span> <span class="o">*</span> <span class="n">x_1</span>
    
    <span class="c1"># 4. 목표 벡터장 (x_1 - x_0)
</span>    <span class="n">target_v</span> <span class="o">=</span> <span class="n">x_1</span> <span class="o">-</span> <span class="n">x_0</span>
    
    <span class="c1"># 5. 모델 예측 및 손실 계산
</span>    <span class="n">pred_v</span> <span class="o">=</span> <span class="nf">v_net</span><span class="p">(</span><span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
    <span class="n">loss</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">mean</span><span class="p">((</span><span class="n">pred_v</span> <span class="o">-</span> <span class="n">target_v</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">)</span>
    
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div>

<hr />

<h2 id="4-로봇-공학-응용-pi-model">4. 로봇 공학 응용: Pi Model</h2>

<p>Flow Matching은 로봇의 <strong>Policy Inference (Pi Model)</strong>에서 다봉성(Multi-modality)을 가진 행동 분포를 모델링하는 데 사용됩니다.</p>

<ul>
  <li><strong>Pi Model ($\pi(a \mid s)$)</strong>: 주어진 상태 $s$에서 최적의 행동 $a$를 생성하는 모델입니다.</li>
  <li><strong>장점</strong>: Diffusion Policy에 비해 추론 시 적은 step(Iteration)으로도 고품질의 행동 시퀀스를 생성할 수 있어, 실시간 제어가 중요한 로봇 시스템에 적합합니다.</li>
  <li><strong>Flow</strong>: 노이즈 $a_0$로부터 시작하여 벡터장을 따라 적분함으로써 최종 행동 $a_1$을 도출합니다.</li>
</ul>

<hr />

<h2 id="5-연습-문제">5. 연습 문제</h2>

<h3 id="q1-조건부-벡터장-유도">Q1. 조건부 벡터장 유도</h3>

<p>선형 보간 경로가 다음과 같이 정의된다:</p>

\[x_t = (1-t)x_0 + t x_1, \qquad t \in [0, 1]\]

<p><strong>(a)</strong> $x_t$를 시간 $t$에 대해 미분하여 조건부 벡터장 $u_t(x_t \mid x_1)$를 구하라.</p>

<p><strong>(b)</strong> $t=0$과 $t=1$에서의 $u_t$ 값이 각각 $x_1 - x_0$로 동일한 이유를 설명하라.</p>

<details>
<summary>정답 보기</summary>

**(a)**

$$u_t(x_t \mid x_1) = \frac{d}{dt}x_t = \frac{d}{dt}\left[(1-t)x_0 + tx_1\right] = -x_0 + x_1 = x_1 - x_0$$

**(b)** 경로가 **선형**이므로 기울기(속도)가 $t$에 무관하게 상수 $x_1 - x_0$다. 직선 위를 일정한 속도로 이동하기 때문에 출발점($t=0$)과 도착 직전($t=1$) 모두 같은 방향·크기의 벡터장을 가진다.

</details>

<hr />

<h3 id="q2-손실-함수-계산-수치-예시">Q2. 손실 함수 계산 (수치 예시)</h3>

<p>$x_0 = -1$, $x_1 = 3$, $t = 0.25$일 때:</p>

<p><strong>(a)</strong> $x_t$를 계산하라.</p>

<p><strong>(b)</strong> 목표 벡터장 $u_t(x_t \mid x_1)$를 계산하라.</p>

<p><strong>(c)</strong> 모델 예측이 $v_\theta(x_t, t) = 3.5$일 때, CFM 손실 $| v_\theta - u_t |^2$를 계산하라.</p>

<details>
<summary>정답 보기</summary>

**(a)**

$$x_{0.25} = (1 - 0.25)(-1) + 0.25(3) = -0.75 + 0.75 = 0$$

**(b)**

$$u_t = x_1 - x_0 = 3 - (-1) = 4$$

**(c)**

$$\|v_\theta - u_t\|^2 = |3.5 - 4|^2 = (-0.5)^2 = 0.25$$

</details>

<hr />

<h3 id="q3-euler-방법으로-샘플링">Q3. Euler 방법으로 샘플링</h3>

<p>훈련된 모델이 벡터장 $v_\theta(x, t) = 2x + 1$을 출력한다고 하자. 초기값 $x_0 = 0$에서 시작하여 스텝 크기 $\Delta t = 0.5$로 Euler 방법을 2번 반복 적용하라:</p>

\[x_{t + \Delta t} = x_t + \Delta t \cdot v_\theta(x_t,\, t)\]

<p><strong>(a)</strong> $x_{0.5}$를 구하라.</p>

<p><strong>(b)</strong> $x_{1.0}$을 구하라.</p>

<p><strong>(c)</strong> 스텝 수를 늘릴수록 ($\Delta t \to 0$) 결과가 어떻게 변하는지 설명하라.</p>

<details>
<summary>정답 보기</summary>

**(a)**

$$x_{0.5} = x_0 + 0.5 \cdot v_\theta(x_0, 0) = 0 + 0.5 \cdot (2 \cdot 0 + 1) = 0.5$$

**(b)**

$$x_{1.0} = x_{0.5} + 0.5 \cdot v_\theta(x_{0.5}, 0.5) = 0.5 + 0.5 \cdot (2 \cdot 0.5 + 1) = 0.5 + 1.0 = 1.5$$

**(c)** $\Delta t \to 0$이 되면 Euler 방법이 ODE $\dot{x} = v_\theta(x, t)$의 정확한 해에 수렴한다. 스텝이 클수록 각 구간에서 벡터장이 변함에도 불구하고 처음 값으로 직선 이동하는 오차가 누적된다. 따라서 스텝 수가 많을수록(더 촘촘하게 적분할수록) 샘플 품질이 좋아진다.

</details>

<hr />

<h3 id="q4-연속-방정식-continuity-equation">Q4. 연속 방정식 (Continuity Equation)</h3>

<p>확률 밀도 $p_t$는 벡터장 $v_t$에 의해 다음 연속 방정식을 만족해야 한다:</p>

\[\frac{\partial p_t}{\partial t} + \nabla_x \cdot (p_t(x)\, v_t(x)) = 0\]

<p>1차원에서 $p_0 = \mathcal{N}(0, 1)$, $p_1 = \mathcal{N}(\mu, 1)$ ($\mu \neq 0$)이라 하자.</p>

<p><strong>(a)</strong> 선형 경로 $x_t = (1-t)x_0 + t\mu$에 대응하는 벡터장 $v_t(x)$를 구하라.</p>

<p><strong>(b)</strong> 이 $v_t$가 연속 방정식을 만족함을 확인하라. (Hint: $p_t = \mathcal{N}(t\mu,\,(1-t)^2)$)</p>

<details>
<summary>정답 보기</summary>

**(a)** $x_t = (1-t)x_0 + t\mu$이므로 $x_0 = \dfrac{x_t - t\mu}{1-t}$. 조건부 벡터장을 $t$에 대해 미분하면:

$$v_t(x) = \frac{d}{dt}x_t = \mu - x_0 = \mu - \frac{x - t\mu}{1-t} = \frac{\mu - x}{1-t}$$

**(b)** $p_t(x) = \mathcal{N}(t\mu,\,(1-t)^2)$로 쓰면:

$$\frac{\partial p_t}{\partial t} = p_t(x) \cdot \frac{x - t\mu}{(1-t)^2}\,\mu - \frac{p_t(x)}{-(1-t)}\cdot\frac{(x-t\mu)^2-(1-t)^2}{(1-t)^2}$$

계산 과정을 요약하면:

$$\frac{\partial p_t}{\partial x}(p_t \cdot v_t) = \frac{1}{1-t}\frac{\partial}{\partial x}\left[(\mu - x)\,p_t\right] = \frac{1}{1-t}\left[-p_t + (\mu-x)\frac{\partial p_t}{\partial x}\right]$$

$\dfrac{\partial p_t}{\partial x} = -\dfrac{x - t\mu}{(1-t)^2}p_t$를 대입하면 두 항이 정확히 상쇄되어 연속 방정식 $= 0$이 성립한다.

</details>

<hr />

<h3 id="q5-주변-벡터장-marginal-vector-field">Q5. 주변 벡터장 (Marginal Vector Field)</h3>

<p>Flow Matching의 핵심 이론적 결과는 다음이다:</p>

\[v_t(x) = \mathbb{E}\!\left[u_t(x \mid x_1) \;\middle|\; x_t = x\right]\]

<p><strong>(a)</strong> 위 등식의 의미를 직관적으로 설명하라. (조건부 기댓값이 왜 “평균적으로 올바른” 방향을 가리키는가?)</p>

<p><strong>(b)</strong> CFM 손실 $\mathcal{L}_{CFM}$를 최소화하는 것이 위 주변 벡터장 $v_t$를 학습하는 것과 동치임을 설명하라.</p>

<details>
<summary>정답 보기</summary>

**(a)** 시각 $t$에 같은 위치 $x$를 지나는 궤적은 여러 개의 서로 다른 목적지 $x_1$에서 비롯될 수 있다. 각 궤적은 자신의 조건부 벡터장 $u_t(x \mid x_1)$를 갖는데, 주변 벡터장 $v_t(x)$는 "현재 $x_t = x$를 지나고 있다"는 조건 아래 이 방향들의 가중 평균이다. 이 평균 방향으로 이동하면 **전체 확률 분포를 올바르게 수송**할 수 있다.

**(b)** MSE 손실 $\mathbb{E}\|v_\theta(x_t, t) - u_t(x_t \mid x_1)\|^2$를 $v_\theta$에 대해 최소화하면, 각 $x_t$ 지점에서의 최적 예측은 조건부 기댓값 $\mathbb{E}[u_t \mid x_t]$이다. 이것이 바로 주변 벡터장 $v_t(x_t)$이므로, CFM 손실을 최소화하는 $v_\theta$는 $v_t$를 학습한다.

</details>

<hr />

<h3 id="q6-diffusion-vs-flow-matching-비교">Q6. Diffusion vs Flow Matching 비교</h3>

<p>Diffusion 모델의 forward process는 다음과 같다:</p>

\[x_t = \sqrt{\bar{\alpha}_t}\, x_0 + \sqrt{1 - \bar{\alpha}_t}\, \epsilon, \qquad \epsilon \sim \mathcal{N}(0, I)\]

<p>반면 Flow Matching의 경로는 $x_t = (1-t)x_0 + t x_1$이다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>Diffusion</th>
      <th>Flow Matching</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>경로 형태</td>
      <td>비선형 (noise schedule)</td>
      <td>선형 보간</td>
    </tr>
    <tr>
      <td>목표 함수</td>
      <td>$\epsilon$-prediction (노이즈 예측)</td>
      <td>$v$-prediction (벡터장 예측)</td>
    </tr>
    <tr>
      <td>추론 step 수</td>
      <td>수십~수백</td>
      <td>수십 이하 가능</td>
    </tr>
    <tr>
      <td>ODE Solver 필요 여부</td>
      <td>?</td>
      <td>?</td>
    </tr>
  </tbody>
</table>

<p><strong>(a)</strong> 표의 빈칸을 채워라.</p>

<p><strong>(b)</strong> Flow Matching이 Diffusion보다 적은 추론 step으로도 좋은 성능을 낼 수 있는 이유를 경로의 기하학적 관점에서 설명하라.</p>

<details>
<summary>정답 보기</summary>

**(a)**

| 항목 | Diffusion | Flow Matching |
|---|---|---|
| 경로 형태 | 비선형 (noise schedule) | 선형 보간 |
| 목표 함수 | $\epsilon$-prediction (노이즈 예측) | $v$-prediction (벡터장 예측) |
| 추론 step 수 | 수십~수백 | 수십 이하 가능 |
| ODE Solver 필요 여부 | 필요 (DDIM 등 ODE sampler) | 필요 (Euler / RK4 등) |

**(b)** Diffusion은 noise schedule로 인해 궤적이 **곡선**을 그리므로, 이를 충실하게 수치 적분하려면 많은 스텝이 필요하다. 반면 Flow Matching의 선형 보간 경로는 **직선**이다. 직선 궤적은 Euler 방법 같은 1차 적분기로도 큰 오차 없이 따라갈 수 있으므로, 적은 함수 평가(NFE, Number of Function Evaluations)로 동등한 품질의 샘플을 생성할 수 있다.

</details>]]></content><author><name></name></author><category term="study" /><category term="FlowMatching" /><category term="GenerativeModel" /><category term="Robotics" /><category term="Pi Model" /><summary type="html"><![CDATA[Flow Matching의 핵심 개념과 수학적 구조, 그리고 로봇 제어(Pi Model)로의 응용을 정리합니다.]]></summary></entry><entry><title type="html">VAE 이해</title><link href="https://daye-lee18.github.io/blog/2026/VAE/" rel="alternate" type="text/html" title="VAE 이해" /><published>2026-05-06T00:00:00+00:00</published><updated>2026-05-06T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2026/VAE</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2026/VAE/"><![CDATA[<hr />
<h2 id="1-vae-variational-autoencoder-요약">1. VAE (Variational Autoencoder) 요약</h2>

<p>VAE는 데이터를 생성하는 잠재 변수(Latent Variable) 모델로, 데이터 $x$의 분포 $p(x)$를 학습하는 생성 모델입니다.</p>

<hr />
<h3 id="핵심-개념">핵심 개념</h3>

<p>VAE는 관측 데이터 $x$가 직접 관측되지 않는 잠재 변수 $z$로부터 생성되었다고 가정하는 <strong>잠재 변수 모델(Latent Variable Model)</strong>입니다 ($z \to x$). 이때 실제 posterior $p(z \mid x)$를 직접 계산하는 것은 불가능(Intractable)합니다. 왜냐하면 $p(x) = \int p(x \mid z)\,p(z)\,dz$를 모든 $z$에 대해 적분하는 것이 매우 어렵기 때문입니다.</p>

<p>이를 해결하기 위해 <strong>Variational Inference</strong>를 사용합니다. 다루기 어려운 $p(z \mid x)$ 대신, 다루기 쉬운 분포 $q_\phi(z \mid x)$(주로 가우시안)를 도입하여 $p(z \mid x)$를 근사합니다.</p>

<hr />
<h3 id="kl-divergence-kullback-leibler-divergence-란">KL Divergence (Kullback-Leibler Divergence) 란?</h3>

<p>KL Divergence는 두 확률 분포의 <strong>차이(Difference)</strong>를 측정하는 지표입니다. 분포 $P$와 $Q$ 사이의 KL Divergence는 다음과 같이 정의됩니다.</p>

\[D_{KL}(P \| Q) = \mathbb{E}_{x \sim P} \left[ \log \frac{P(x)}{Q(x)} \right] = \int P(x) \log \frac{P(x)}{Q(x)}\, dx\]

<p>KL Divergence는 <strong>항상 0 이상</strong>이며, 두 분포가 완전히 같을 때만 0이 됩니다(Gibbs’ Inequality). 또한 $D_{KL}(P | Q) \neq D_{KL}(Q | P)$인 <strong>비대칭(Non-symmetric)</strong> 지표이므로 엄밀한 의미의 거리(Metric) 함수가 아니며, <strong>상대 엔트로피(Relative Entropy)</strong>라고도 불립니다. VAE에서 KL Divergence는 인코더가 생성한 잠재 변수 분포 $q_\phi(z \mid x)$가 사전 분포 $p(z)$(표준 정규분포)에서 크게 벗어나지 않도록 강제하는 <strong>규제화(Regularization)</strong> 역할을 합니다.</p>

<hr />
<h3 id="목적-함수-elbo-evidence-lower-bound">목적 함수: ELBO (Evidence Lower Bound)</h3>

<p>데이터의 로그 우도 $\log p(x)$를 직접 최대화하는 대신, 그 하한선인 ELBO를 최대화합니다.</p>

\[\log p(x) \geq \underbrace{\mathbb{E}_{q_\phi(z \mid x)}\!\left[\log p_\theta(x \mid z)\right]}_{\text{Reconstruction Loss}} - \underbrace{D_{KL}(q_\phi(z \mid x) \| p(z))}_{\text{Regularization Loss}}\]

<p>첫 번째 항(Reconstruction Loss)은 $z$로부터 $x$를 얼마나 잘 복원하는지를, 두 번째 항(Regularization Loss)은 근사 posterior $q_\phi(z \mid x)$가 사전 분포 $p(z)$와 얼마나 유사한지를 측정합니다.</p>

<hr />

<h2 id="2-mle-maximum-likelihood-estimation-문제">2. MLE (Maximum Likelihood Estimation) 문제</h2>

<p><strong>[문제]</strong> 독립적인 $n$개의 데이터 $X = {x_1, x_2, \dots, x_n}$이 평균이 $\mu$이고 분산이 $\sigma^2$인 정규분포 $\mathcal{N}(\mu, \sigma^2)$에서 추출되었다고 가정할 때, $\mu$와 $\sigma^2$의 최대 우도 추정치(MLE)를 구하시오.</p>

<p><strong>[풀이]</strong></p>
<ol>
  <li>
    <p>우도 함수(Likelihood function) $L(\mu, \sigma^2)$ 정의:</p>

\[L(\mu, \sigma^2) = \prod_{i=1}^n \frac{1}{\sqrt{2\pi\sigma^2}} \exp\left(-\frac{(x_i-\mu)^2}{2\sigma^2}\right)\]
  </li>
  <li>
    <p>로그 우도 함수(Log-likelihood function) $l(\mu, \sigma^2)$ 계산:</p>

\[l(\mu, \sigma^2) = -\frac{n}{2}\log(2\pi) - \frac{n}{2}\log(\sigma^2) - \frac{1}{2\sigma^2}\sum_{i=1}^n (x_i-\mu)^2\]
  </li>
  <li>
    <p>$\mu$에 대해 편미분하여 0이 되는 지점 찾기:</p>

\[\frac{\partial l}{\partial \mu} = \frac{1}{\sigma^2}\sum_{i=1}^n (x_i-\mu) = 0 \implies \hat{\mu} = \frac{1}{n}\sum_{i=1}^n x_i \quad \text{(표본평균)}\]
  </li>
  <li>
    <p>$\sigma^2$에 대해 편미분하여 0이 되는 지점 찾기:</p>

\[\frac{\partial l}{\partial \sigma^2} = -\frac{n}{2\sigma^2} + \frac{1}{2(\sigma^2)^2}\sum_{i=1}^n (x_i-\mu)^2 = 0 \implies \hat{\sigma}^2 = \frac{1}{n}\sum_{i=1}^n (x_i-\hat{\mu})^2\]
  </li>
</ol>

<hr />

<h2 id="3-vaeelbo-유도-문제">3. VAE/ELBO 유도 문제</h2>

<p><strong>[문제]</strong> 데이터 $x$와 잠재 변수 $z$에 대하여, 근사 분포 $q(z \mid x)$와 실제 Posterior $p(z \mid x)$ 사이의 KL Divergence를 이용하여 ELBO 식을 유도하고, 왜 ELBO를 최대화하는 것이 $\log p(x)$를 최대화하는 것과 연관되는지 설명하시오.</p>

<p><strong>[풀이]</strong></p>

<ol>
  <li>
    <p>KL Divergence의 정의로부터 시작합니다:</p>

\[D_{KL}(q(z \mid x) \| p(z \mid x)) = \mathbb{E}_{q(z \mid x)} \left[ \log \frac{q(z \mid x)}{p(z \mid x)} \right]\]
  </li>
  <li>
    <p>베이즈 정리 $p(z \mid x) = \frac{p(x, z)}{p(x)}$를 대입합니다:</p>

\[D_{KL}(q(z \mid x) \| p(z \mid x)) = \mathbb{E}_{q(z \mid x)} \left[ \log \frac{q(z \mid x) \cdot p(x)}{p(x, z)} \right]\]

\[= \mathbb{E}_{q(z \mid x)} \left[ \log q(z \mid x) - \log p(x, z) + \log p(x) \right]\]
  </li>
  <li>
    <p>$\log p(x)$는 $z$에 의존하지 않으므로 기댓값 밖으로 나옵니다:</p>

\[D_{KL}(q(z \mid x) \| p(z \mid x)) = \log p(x) + \mathbb{E}_{q(z \mid x)} \left[ \log q(z \mid x) - \log p(x, z) \right]\]
  </li>
  <li>
    <p>식을 $\log p(x)$에 대해 정리합니다:</p>

\[\log p(x) = D_{KL}(q(z \mid x) \| p(z \mid x)) + \mathbb{E}_{q(z \mid x)} \left[ \log p(x, z) - \log q(z \mid x) \right]\]
  </li>
  <li>
    <p>우측의 기댓값 항이 바로 <strong>ELBO</strong>입니다. $p(x, z) = p(x \mid z)\,p(z)$이므로:</p>

\[\boxed{\text{ELBO} = \mathbb{E}_{q(z \mid x)} \left[ \log p(x \mid z) \right] - D_{KL}(q(z \mid x) \| p(z))}\]
  </li>
</ol>

<p><strong>[설명]</strong>
$D_{KL}$은 항상 0 이상이므로 $\log p(x) \geq \text{ELBO}$가 성립합니다. 따라서 ELBO는 $\log p(x)$의 하한선(Lower Bound)이 됩니다. $D_{KL}(q(z \mid x) | p(z \mid x))$를 최소화하는 것(근사 분포를 실제 posterior에 가깝게 만드는 것)은 곧 ELBO를 최대화하는 것과 같으며, 이는 데이터의 우도 $\log p(x)$를 간접적으로 최대화하는 효과를 가집니다.</p>]]></content><author><name></name></author><category term="study" /><category term="VAE" /><category term="ELBO" /><category term="KL divergence" /><category term="MLE" /><summary type="html"><![CDATA[VAE의 핵심 개념인 ELBO와 KL Divergence, 그리고 수학적 유도 과정을 정리합니다.]]></summary></entry><entry><title type="html">Asyncio</title><link href="https://daye-lee18.github.io/blog/2026/asyncio/" rel="alternate" type="text/html" title="Asyncio" /><published>2026-04-29T00:00:00+00:00</published><updated>2026-04-29T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2026/asyncio</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2026/asyncio/"><![CDATA[<h1 id="asyncio-사용하기">asyncio 사용하기</h1>

<blockquote>
  <p>asyncio (Asynchronous I/O)는 비동기 프로그래밍을 위한 모듈이며 I/O 작업을 효율적으로 동시에 처리하게 해줌</p>

  <p>동기 (synchronous) 처리는 특정 작업이 끝나면 다음 작업을 처리하는 순차처리 방식이고, 비동기 (asynchronous) 처리는 여러 작업을 처리하도록 예약한 뒤 작업이 끝나면 결과를 받는 방식</p>
</blockquote>

<p class="text-center">
  <img src="/assets/img/blogs/async.png" class="img-fluid rounded z-depth-1" style="max-width: 520px; width: 100%; height: auto;" alt="asyncio event loop diagram" loading="eager" />
</p>

<h2 id="필수-개념">필수 개념</h2>

<ul>
  <li>파이썬에서는 제너레이터 기반(yield / yield from)의 코루틴과 구분하기 위해 async def로 만든 코루틴은 네이티브 코루틴이라고 함
    <ul>
      <li>python 3.5 부터 <code class="language-plaintext highlighter-rouge">async def</code> / <code class="language-plaintext highlighter-rouge">await</code> 문법이 언어 차원에서 정식 지원되어 “네이티브 (native)”라 명명</li>
      <li>코루틴 (coroutine): 다른 실행 흐름과 협력 (co) 하면서 번갈아 실행되는 함수라는 의미</li>
    </ul>
  </li>
  <li>async def 키워드는 파이썬 3.5 이상부터 사용 가능 (이전까지 데코레이터 @asyncio.coroutine 사용)</li>
  <li>이벤트 루프 (event loop): 비동기 작업들을 관리하는 실행 관리자
    <ul>
      <li>await keyword: <code class="language-plaintext highlighter-rouge">await</code>를 만나면 현재 코루틴은 일시 중단되고, 이벤트 루프는 해당 작업이 끝났을 때 다시 실행할 수 있도록 기록함</li>
      <li>기다리는 동안 이벤트 루프는 멈추지 않고 실행 가능한 다른 Task나 콜백을 처리함</li>
      <li><code class="language-plaintext highlighter-rouge">loop</code>처럼 계속 돌면서 실행할 작업을 찾는다.</li>
    </ul>
  </li>
</ul>

<details><summary>yield, yield from, await</summary>
<ul>
  <li>yield: 값을 하나 내보내고 멈춤, 다음 next()가 실행될때까지</li>
  <li>yield from: 다른 제너레이터/이터러블에 위임해서 그 안의 yield들을 바깥으로 전달함</li>
  <li>asyncio의 yield from: await이 나오기 전, 비동기 작업을 기다리는 지점으로 사용됨</li>
  <li>await: 현재 코루틴을 멈추고, 이벤트 루프에게 제어권을 넘긴 뒤, 작업 완료 후 이어서 실행</li>
</ul>

</details>

<blockquote class="block-tip">
  <h5 id="event-loop-의-동작-원리">event loop 의 동작 원리</h5>

  <ol>
    <li>실행 가능한 Task나 콜백을 실행한다</li>
    <li>await를 만나면 잠시 멈춘다</li>
    <li>I/O, 타이머, 네트워크 응답 등을 기다린다</li>
    <li>준비된 Task를 다시 이어서 실행한다</li>
    <li>이 과정을 반복한다</li>
  </ol>
</blockquote>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">asyncio</span>

<span class="c1"># old style
</span><span class="nd">@asyncio.coroutine</span>
<span class="k">def</span> <span class="nf">old_coro</span><span class="p">():</span>
    <span class="n">result</span> <span class="o">=</span> <span class="k">yield</span> <span class="k">from</span> <span class="nf">some_io</span><span class="p">()</span> <span class="c1"># 이 작업이 끝날 때까지 현재 코루틴을 멈추고 이벤트 루프에 맡긴다.
</span>    <span class="k">return</span> <span class="n">result</span>

<span class="c1"># yield를 사용하는 일반 제너레이터 예시
</span><span class="k">def</span> <span class="nf">numbers</span><span class="p">():</span>
    <span class="k">yield</span> <span class="mi">1</span>
    <span class="k">yield</span> <span class="mi">2</span>

<span class="n">g</span> <span class="o">=</span> <span class="nf">numbers</span><span class="p">()</span>
<span class="nf">next</span><span class="p">(</span><span class="n">g</span><span class="p">)</span> <span class="c1"># 1을 반환하고 함수 실행이 멈춤
</span><span class="nf">next</span><span class="p">(</span><span class="n">g</span><span class="p">)</span> <span class="c1"># 멈췄던 자리부터 이어서 실행되어 2를 반환함
</span>
<span class="c1"># new style
</span><span class="k">async</span> <span class="k">def</span> <span class="nf">fetch_data</span><span class="p">():</span> <span class="c1"># 코루틴 함수
</span>    <span class="k">await</span> <span class="nf">some_io</span><span class="p">()</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">done</span><span class="sh">"</span>

<span class="nf">fetch_data</span><span class="p">()</span> <span class="c1"># 코루틴 객체
</span></code></pre></div></div>

<h2 id="native-coroutine-생성">native coroutine 생성</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">async def</code> 함수를 호출하면 코루틴 객체가 만들어짐</li>
  <li>코루틴 객체는 생성만으로 실행되지 않고, 이벤트 루프가 실행해야 함</li>
  <li>동기 코드에서 비동기 코드를 실행할 때 이벤트 루프를 얻고 <code class="language-plaintext highlighter-rouge">run_until_complete()</code>로 코루틴을 실행할 수 있음</li>
</ul>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">asyncio</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">hello</span><span class="p">():</span> <span class="c1"># async def로 네이티브 코루틴을 만듦. 
</span>    <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">Hello, world!</span><span class="sh">'</span><span class="p">)</span>

<span class="n">loop</span> <span class="o">=</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">get_event_loop</span><span class="p">()</span>  <span class="c1"># 이벤트 루프를 얻음 
</span><span class="n">loop</span><span class="p">.</span><span class="nf">run_until_complete</span><span class="p">(</span><span class="nf">hello</span><span class="p">())</span> <span class="c1"># hello가 끝날 때까지 기다림 
</span><span class="n">loop</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span> <span class="c1"># 이벤트 루프를 닫음 
</span></code></pre></div></div>

<p>실험 결과</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hello, world!
</code></pre></div></div>

<blockquote class="block-tip">
  <h5 id="주요-api">주요 API</h5>

  <ul>
    <li><code class="language-plaintext highlighter-rouge">get_event_loop()</code>: 현재 스레드에서 사용할 이벤트 루프 객체를 가져옴. 이 함수만 호출한다고 코루틴이 실행되지는 않음.</li>
    <li><code class="language-plaintext highlighter-rouge">run_until_complete()</code>: 전달받은 코루틴 객체를 이벤트 루프가 관리할 수 있는 작업으로 감싸 실행하고, 그 작업이 완료될 때까지 이벤트 루프를 돌림.</li>
    <li><code class="language-plaintext highlighter-rouge">close()</code>: 이벤트 루프 사용이 끝난 뒤 관련 자원을 정리함.</li>
  </ul>
</blockquote>

<p>즉 <code class="language-plaintext highlighter-rouge">run_until_complete()</code>는 동기 코드에서 비동기 코드로 들어가는 진입점 역할을 함. 요즘 코드는 보통 이벤트 루프를 직접 얻고 닫기보다 <code class="language-plaintext highlighter-rouge">asyncio.run()</code>을 사용함.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">hello</span><span class="p">())</span>
</code></pre></div></div>

<p>동기 코드와 비동기 코드가 함께 있을 때는 보통 동기 코드가 바깥에서 시작하고, 특정 지점에서 이벤트 루프를 실행해 비동기 코드를 처리함. 이벤트 루프 안에서는 <code class="language-plaintext highlighter-rouge">await</code>를 만난 코루틴이 멈추는 동안 다른 Task나 콜백이 실행될 수 있음.</p>

<p>단, 오래 걸리는 일반 동기 함수가 이벤트 루프 안에서 그대로 실행되면 그동안 다른 비동기 작업도 멈출 수 있음. 이런 블로킹 작업은 별도 스레드로 넘겨 실행할 수 있음.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">to_thread</span><span class="p">(</span><span class="n">blocking_func</span><span class="p">)</span>
</code></pre></div></div>

<h2 id="await-로-네이티브-코루틴-실행하기">await 로 네이티브 코루틴 실행하기</h2>

<ul>
  <li>다음과 같이 <code class="language-plaintext highlighter-rouge">await</code> 뒤에 코루틴 객체, Future 객체, Task 객체 같은 awaitable 객체를 지정하면 해당 객체가 끝날 때까지 현재 코루틴이 일시 중단됨</li>
  <li>이때 이벤트 루프는 멈추지 않고, 실행 가능한 다른 Task나 콜백을 처리함</li>
  <li>await한 객체가 완료되면 이벤트 루프는 중단된 코루틴을 다시 실행하고, 완료 결과를 <code class="language-plaintext highlighter-rouge">await</code> 표현식의 값으로 반환함</li>
  <li><code class="language-plaintext highlighter-rouge">await</code>는 단어 뜻 그대로 특정 객체가 끝날 때까지 기다리는 것처럼 보이지만, 실제로는 현재 코루틴만 멈추고 이벤트 루프에 제어권을 넘기는 것</li>
  <li><code class="language-plaintext highlighter-rouge">await</code> 키워드는 파이썬 3.5 이상부터 사용 가능, 3.4 에서는 <code class="language-plaintext highlighter-rouge">yield from</code>을 사용</li>
  <li>주의점: <code class="language-plaintext highlighter-rouge">await</code>는 네이티브 코루틴 안에서만 사용할 수 있음.</li>
</ul>

<p>이벤트 루프가 네이티브 코루틴 객체만 직접 실행하는 것은 아님. 보통 코루틴은 <code class="language-plaintext highlighter-rouge">Task</code>로 감싸져 이벤트 루프에 의해 스케줄링되고, 이벤트 루프는 Future, Task, 콜백, 타이머, I/O 이벤트 등을 함께 관리함.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>구분</th>
      <th>생성/등록 함수</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>코루틴 객체</td>
      <td><code class="language-plaintext highlighter-rouge">async def</code> 함수 호출</td>
      <td>아직 실행되지 않은 비동기 작업 객체를 만듦. 단독으로는 실행되지 않고 <code class="language-plaintext highlighter-rouge">await</code>하거나 Task로 등록해야 함</td>
    </tr>
    <tr>
      <td>Task</td>
      <td><code class="language-plaintext highlighter-rouge">asyncio.create_task(coro)</code></td>
      <td>코루틴 객체를 이벤트 루프에 스케줄링함. 다른 Task와 번갈아 실행될 수 있음</td>
    </tr>
    <tr>
      <td>Future</td>
      <td><code class="language-plaintext highlighter-rouge">loop.create_future()</code></td>
      <td>미래에 결과가 채워질 객체를 만듦. 보통 저수준 API나 라이브러리 내부에서 사용됨</td>
    </tr>
    <tr>
      <td>콜백</td>
      <td><code class="language-plaintext highlighter-rouge">loop.call_soon(callback)</code></td>
      <td>일반 함수를 이벤트 루프의 다음 실행 기회에 호출하도록 등록함</td>
    </tr>
    <tr>
      <td>타이머</td>
      <td><code class="language-plaintext highlighter-rouge">loop.call_later(seconds, callback)</code><br /><code class="language-plaintext highlighter-rouge">asyncio.sleep(seconds)</code></td>
      <td>일정 시간이 지난 뒤 콜백이나 코루틴이 다시 실행되도록 예약함</td>
    </tr>
    <tr>
      <td>I/O 이벤트</td>
      <td><code class="language-plaintext highlighter-rouge">asyncio.open_connection()</code><br /><code class="language-plaintext highlighter-rouge">asyncio.start_server()</code></td>
      <td>네트워크 연결, 서버 소켓 같은 I/O 작업을 이벤트 루프가 감시하고 준비되면 처리함</td>
    </tr>
  </tbody>
</table>

<p>일반 함수도 콜백으로 등록하면 이벤트 루프가 실행할 수 있음. 하지만 일반 함수는 <code class="language-plaintext highlighter-rouge">await</code>로 제어권을 넘길 수 없기 때문에, 오래 걸리는 동기 작업을 그대로 실행하면 이벤트 루프를 막을 수 있음.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>변수 = await 코루틴 객체 
변수 = await Future 객체
변수 = await Task 객체
</code></pre></div></div>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">asyncio</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">a</span><span class="p">():</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">a start</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">await</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">a end</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">A</span><span class="sh">"</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">b</span><span class="p">():</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">b start</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">await</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">b end</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">B</span><span class="sh">"</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="n">task1</span> <span class="o">=</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">create_task</span><span class="p">(</span><span class="nf">a</span><span class="p">())</span>
    <span class="n">task2</span> <span class="o">=</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">create_task</span><span class="p">(</span><span class="nf">b</span><span class="p">())</span>

    <span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="n">task1</span> <span class="c1"># task1이 끝날 때까지 main 코루틴은 멈춤
</span>    <span class="nf">print</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>

    <span class="k">await</span> <span class="n">task2</span>

<span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">main</span><span class="p">())</span>
</code></pre></div></div>

<p>실행 흐름</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a start
b start
... 1초 대기 ...
a end
b end
A
</code></pre></div></div>]]></content><author><name></name></author><category term="study" /><category term="asyncio" /><category term="asychronous" /><summary type="html"><![CDATA[Asyncio python]]></summary></entry><entry><title type="html">Git 팀 가이드 허브</title><link href="https://daye-lee18.github.io/blog/2026/git-team-guide/" rel="alternate" type="text/html" title="Git 팀 가이드 허브" /><published>2026-04-21T00:00:00+00:00</published><updated>2026-04-21T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2026/git-team-guide</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2026/git-team-guide/"><![CDATA[<h2 id="어디를-보면-되나요">어디를 보면 되나요</h2>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>역할</th>
      <th>문서</th>
      <th>무엇을 보면 되나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>팀원</td>
      <td><a href="/blog/2026/git-team-member-guide/">팀원 문서</a></td>
      <td>최신화, 브랜치 생성, 커밋, push, PR</td>
    </tr>
    <tr>
      <td>팀장</td>
      <td><a href="/blog/2026/git-team-lead-guide/">팀장 문서</a></td>
      <td>PR 리뷰, squash merge, 브랜치 삭제, 제출</td>
    </tr>
  </tbody>
</table>

<h2 id="왜-나눴나">왜 나눴나</h2>

<p>한 문서에 모든 내용을 넣으면 팀원이 읽을 때 너무 길어집니다.
기본 작업은 팀원 문서만 보면 되고, 리뷰와 배포 책임은 팀장 문서만 보면 되도록 분리했습니다.</p>

<h2 id="다음에-볼-문서">다음에 볼 문서</h2>

<ul>
  <li><a href="/blog/2026/git-team-member-guide/">팀원 문서</a>에서 팀원 작업 흐름을 읽습니다.</li>
  <li><a href="/blog/2026/git-team-lead-guide/">팀장 문서</a>에서 팀장 흐름을 읽습니다.</li>
</ul>]]></content><author><name></name></author><category term="guide" /><category term="git" /><category term="team" /><summary type="html"><![CDATA[팀원용과 팀장용 Git 가이드를 나눠서 바로 들어갈 수 있는 안내 페이지]]></summary></entry><entry><title type="html">Git 팀 사용 가이드 - 팀장</title><link href="https://daye-lee18.github.io/blog/2026/git-team-lead-guide/" rel="alternate" type="text/html" title="Git 팀 사용 가이드 - 팀장" /><published>2026-04-21T00:00:00+00:00</published><updated>2026-04-21T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2026/git-team-lead-guide</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2026/git-team-lead-guide/"><![CDATA[<h2 id="이-문서에서-보는-것">이 문서에서 보는 것</h2>

<ul>
  <li>프로젝트 시작 설정</li>
  <li>PR 리뷰</li>
  <li>squash merge</li>
  <li>버전 태깅과 제출</li>
</ul>

<blockquote class="block-tip">
  <p>팀원용 기본 흐름은 <a href="/blog/2026/git-team-member-guide/">팀원 문서</a>에서 먼저 보고 오면 좋습니다.</p>
</blockquote>

<hr />

<h2 id="phase-1---프로젝트-시작-팀장-1회만">PHASE 1 - 프로젝트 시작 (팀장, 1회만)</h2>

<h3 id="step-1-두-remote-연결">Step 1. 두 remote 연결</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote add origin <span class="o">[</span>우리팀 레포 URL]
git remote add submit <span class="o">[</span>학원 레포 URL]
git remote <span class="nt">-v</span>
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">origin</code>은 우리팀 레포, <code class="language-plaintext highlighter-rouge">submit</code>은 학원 제출용 레포입니다.
<code class="language-plaintext highlighter-rouge">git remote -v</code>로 두 주소가 제대로 연결됐는지 확인합니다.</p>
</blockquote>

<h3 id="step-2-branch-protection-설정">Step 2. Branch Protection 설정</h3>

<p>GitHub 레포 → <strong>Settings</strong> → <strong>Rules → Rulesets</strong> → <strong>New branch ruleset</strong></p>

<h4 id="ruleset-name">Ruleset Name</h4>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>main-protection
</code></pre></div></div>

<h4 id="enforcement-status">Enforcement status</h4>

<p><code class="language-plaintext highlighter-rouge">Active</code> 유지</p>

<h4 id="bypass-list">Bypass list</h4>

<p>상황에 따라 선택합니다.</p>

<ul>
  <li>admin 권한을 회수하는 경우: 비워두기</li>
  <li>admin 권한을 유지하는 경우: 본인 계정 또는 Repository admin 추가</li>
</ul>

<h4 id="target-branches">Target branches</h4>

<p><strong>Add target</strong> 클릭 → <strong>Include by pattern</strong> 선택 → <code class="language-plaintext highlighter-rouge">main</code> 입력 후 Add</p>

<h4 id="branch-rules">Branch rules</h4>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>항목</th>
      <th>설정</th>
      <th>이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Restrict deletions</td>
      <td>✅ 체크</td>
      <td>main 브랜치 실수로 삭제 방지</td>
    </tr>
    <tr>
      <td>Require a pull request before merging</td>
      <td>✅ 체크</td>
      <td>main 직접 push 금지, PR 강제</td>
    </tr>
    <tr>
      <td>Required approvals</td>
      <td><code class="language-plaintext highlighter-rouge">1</code></td>
      <td>팀장 1명 승인 필수</td>
    </tr>
    <tr>
      <td>Dismiss stale pull request approvals</td>
      <td>✅ 권장</td>
      <td>코드 수정 시 재승인 요구</td>
    </tr>
    <tr>
      <td>Block force pushes</td>
      <td>✅ 체크</td>
      <td><code class="language-plaintext highlighter-rouge">git push --force</code> 방지</td>
    </tr>
  </tbody>
</table>

<h3 id="step-3-pr-템플릿-추가">Step 3. PR 템플릿 추가</h3>

<p><code class="language-plaintext highlighter-rouge">.github/PULL_REQUEST_TEMPLATE.md</code> 파일을 만들어 main에 push합니다.</p>

<blockquote class="block-tip">
  <p>이 파일이 레포에 있으면 팀원이 GitHub에서 PR을 만들 때 템플릿이 자동으로 들어갑니다.
보통 <code class="language-plaintext highlighter-rouge">.github/PULL_REQUEST_TEMPLATE.md</code> 위치를 사용합니다.</p>
</blockquote>

<hr />

<h2 id="phase-3---리뷰--병합-팀장">PHASE 3 - 리뷰 &amp; 병합 (팀장)</h2>

<h3 id="step-1-코드-리뷰">Step 1. 코드 리뷰</h3>

<ul>
  <li>PR → Files changed 탭 → 라인별 코멘트</li>
  <li>수정 요청 시: <code class="language-plaintext highlighter-rouge">Request changes</code></li>
  <li>승인 시: <code class="language-plaintext highlighter-rouge">Approve</code></li>
</ul>

<h4 id="pr-변경사항이-너무-많을-때">PR 변경사항이 너무 많을 때</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git branch <span class="nt">-a</span>
</code></pre></div></div>

<p>원격 브랜치가 보이면 로컬에서 확인용 브랜치를 만들어 볼 수 있습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">-b</span> review/casting-factory-from-kim origin/import/casting-factory-from-kim
git checkout main
</code></pre></div></div>

<p>특정 파일만 확인할 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff main...review/casting-factory-from-kim <span class="nt">--</span> path/to/file.py
</code></pre></div></div>

<h4 id="머지된-pr의-변경사항을-로컬에서-확인할-때">머지된 PR의 변경사항을 로컬에서 확인할 때</h4>

<p>GitHub PR 화면에는 브랜치 이름이 이렇게 보일 수 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>refactor/folder-structure &lt;- refactor/db-v23-migration
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">refactor/db-v23-migration</code>은 폴더가 아니라 브랜치 이름입니다.
PR이 이미 머지되었거나 Git Graph에 브랜치가 안 보이면 먼저 원격 히스토리를 최신으로 가져옵니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch <span class="nt">--all</span> <span class="nt">--prune</span>
</code></pre></div></div>

<p>fetch 결과가 아래처럼 나오면,</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>b4f2db5..beb9f15  refactor/folder-structure -&gt; origin/refactor/folder-structure
</code></pre></div></div>

<p>뜻은 원격의 <code class="language-plaintext highlighter-rouge">refactor/folder-structure</code> 브랜치가 <code class="language-plaintext highlighter-rouge">b4f2db5</code>에서 <code class="language-plaintext highlighter-rouge">beb9f15</code>까지 업데이트됐다는 것입니다.</p>

<p>변경 파일 목록만 볼 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff <span class="nt">--name-status</span> b4f2db5..beb9f15
</code></pre></div></div>

<p>파일별 변경량까지 볼 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff <span class="nt">--stat</span> b4f2db5..beb9f15
</code></pre></div></div>

<p>실제 코드 변경 내용까지 볼 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff b4f2db5..beb9f15
</code></pre></div></div>

<p>PR 브랜치 이름으로 비교해도 됩니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff <span class="nt">--name-status</span> b4f2db5..origin/refactor/db-v23-migration
</code></pre></div></div>

<p>특정 파일만 확인할 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff b4f2db5..origin/refactor/db-v23-migration <span class="nt">--</span> server/smart_cast_db/schema/create_tables.sql
</code></pre></div></div>

<p>명령어 구조는 다음과 같습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff <span class="nt">--name-status</span> 시작커밋..끝커밋
</code></pre></div></div>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>예시</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">b4f2db5</code></td>
      <td>PR 변경이 들어가기 전 기준 커밋</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">beb9f15</code></td>
      <td>PR이 머지된 뒤의 머지 커밋</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">origin/refactor/db-v23-migration</code></td>
      <td>PR을 올린 원격 브랜치</td>
    </tr>
  </tbody>
</table>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">--name-status</code>는 파일 목록과 상태만 보여줍니다. <code class="language-plaintext highlighter-rouge">A</code>는 추가, <code class="language-plaintext highlighter-rouge">M</code>은 수정, <code class="language-plaintext highlighter-rouge">D</code>는 삭제, <code class="language-plaintext highlighter-rouge">R</code>은 이름 변경입니다.</p>
</blockquote>

<h3 id="step-2-squash-merge">Step 2. Squash merge</h3>

<p>여러 커밋을 하나로 압축해서 main에 넣는 방식입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>feat/login 브랜치: A -&gt; B -&gt; C -&gt; D -&gt; E
main:               ... -&gt; F
</code></pre></div></div>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>방식</th>
      <th>main 히스토리</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Create a merge commit</td>
      <td>브랜치 커밋 전부 + merge 커밋 추가</td>
      <td>히스토리 전부 보존할 때</td>
    </tr>
    <tr>
      <td>Squash and merge</td>
      <td>커밋 1개로 압축</td>
      <td>팀 작업, main 히스토리 깔끔하게</td>
    </tr>
    <tr>
      <td>Rebase and merge</td>
      <td>브랜치 커밋 전부</td>
      <td>선형 히스토리 유지할 때</td>
    </tr>
  </tbody>
</table>

<h3 id="step-3-브랜치-삭제">Step 3. 브랜치 삭제</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin <span class="nt">--delete</span> feat/기능명
git branch <span class="nt">-d</span> feat/기능명
git branch <span class="nt">-D</span> feat/기능명
</code></pre></div></div>

<blockquote class="block-warning">
  <p>squash merge 후에는 feat 브랜치 역할이 끝납니다.</p>
</blockquote>

<blockquote class="block-tip">
  <p>팀장은 PR merge가 끝나면 원격 브랜치까지 삭제해서 브랜치 목록을 정리합니다.</p>
</blockquote>

<hr />

<h2 id="phase-4---버전-태깅-팀장-마일스톤마다">PHASE 4 - 버전 태깅 (팀장, 마일스톤마다)</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout main
git pull origin main
git tag <span class="nt">-a</span> v0.1 <span class="nt">-m</span> <span class="s2">"v0.1"</span>
git push origin v0.1
</code></pre></div></div>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>태그</th>
      <th>기준</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>v0.1</td>
      <td>핵심 기능 첫 동작</td>
    </tr>
    <tr>
      <td>v0.2</td>
      <td>기능 추가 완료</td>
    </tr>
    <tr>
      <td>v1.0</td>
      <td>최종 제출</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="phase-5---학원-레포-최종-제출-팀장만-제출-시-1회">PHASE 5 - 학원 레포 최종 제출 (팀장만, 제출 시 1회)</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout main
git pull origin main
git push submit main
</code></pre></div></div>

<blockquote class="block-warning">
  <p><code class="language-plaintext highlighter-rouge">submit</code> remote는 제출 목적 전용입니다.</p>
</blockquote>

<hr />

<h2 id="핵심-원칙-요약">핵심 원칙 요약</h2>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>원칙</th>
      <th>이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>main 직접 push 금지</td>
      <td>branch protection으로 강제됨</td>
    </tr>
    <tr>
      <td>작업 시작 전 항상 <code class="language-plaintext highlighter-rouge">git fetch</code></td>
      <td>충돌 방지</td>
    </tr>
    <tr>
      <td>PR 단위를 작게 유지</td>
      <td>리뷰 부담 감소</td>
    </tr>
    <tr>
      <td>Squash merge 사용</td>
      <td>main 히스토리 가독성 유지</td>
    </tr>
    <tr>
      <td>submit은 제출용만</td>
      <td>origin과 용도 분리</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="github-actions-탭-용어">GitHub Actions 탭 용어</h2>

<p><strong>Workflow</strong> - 자동화 작업 정의 파일</p>

<p><strong>Event</strong> - workflow를 트리거한 원인</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
  <span class="na">pull_request</span><span class="pi">:</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>
</code></pre></div></div>

<p><strong>Status</strong> - 실행 결과</p>

<p><strong>Branch</strong> - 어느 브랜치에서 트리거됐는지</p>

<p><strong>Actor</strong> - 누가 트리거했는지</p>]]></content><author><name></name></author><category term="guide" /><category term="git" /><category term="team" /><summary type="html"><![CDATA[팀장이 관리하는 Git 흐름]]></summary></entry><entry><title type="html">Git 팀 사용 가이드 - 팀원</title><link href="https://daye-lee18.github.io/blog/2026/git-team-member-guide/" rel="alternate" type="text/html" title="Git 팀 사용 가이드 - 팀원" /><published>2026-04-21T00:00:00+00:00</published><updated>2026-04-21T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2026/git-team-member-guide</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2026/git-team-member-guide/"><![CDATA[<h2 id="이-문서에서-보는-것">이 문서에서 보는 것</h2>

<ul>
  <li>작업 시작 전 최신화</li>
  <li>브랜치 생성 또는 이어 작업</li>
  <li>커밋, push, PR</li>
</ul>

<h2 id="전체-구조">전체 구조</h2>

<ul>
  <li>우리팀 레포 (<code class="language-plaintext highlighter-rouge">origin</code>): 일상 작업, feat 브랜치, PR, 리뷰</li>
  <li>학원 레포 (<code class="language-plaintext highlighter-rouge">submit</code>): 최종 제출할 때만 push</li>
</ul>

<hr />

<h2 id="매일-반복-작업-사이클">매일 반복 작업 사이클</h2>

<h3 id="step-1-작업-시작-전-최신화">Step 1. 작업 시작 전 최신화</h3>

<p>작업을 시작하기 전에는 항상 원격의 최신 상태를 받아옵니다.
<code class="language-plaintext highlighter-rouge">main</code>을 최신으로 맞춘 뒤 새 브랜치를 만들거나 기존 브랜치로 이동합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git checkout main
git merge origin/main
</code></pre></div></div>

<blockquote class="block-tip">
  <p>여기서 <code class="language-plaintext highlighter-rouge">git merge origin/main</code>은 내 로컬 <code class="language-plaintext highlighter-rouge">main</code>을 최신 상태로 맞추는 작업입니다.
<code class="language-plaintext highlighter-rouge">main</code>에 직접 commit해서 push하는 것과는 다릅니다.</p>
</blockquote>

<p>브랜치 상태를 확인할 때 자주 쓰는 명령어입니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch
git branch <span class="nt">-r</span>
git branch <span class="nt">-a</span>
git log main..origin/main <span class="nt">--oneline</span>
</code></pre></div></div>

<h3 id="step-2-이슈-등록-선택">Step 2. 이슈 등록 (선택)</h3>

<p>이슈는 할 일 목록 / 버그 신고 게시판 역할입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>레포 → Issues 탭 → New issue → 제목 + 내용 작성 → Submit new issue
</code></pre></div></div>

<p>PR 작성 시 <code class="language-plaintext highlighter-rouge">closes #번호</code>를 쓰면 merge될 때 이슈가 자동으로 닫힙니다.</p>

<blockquote class="block-tip">
  <p>이슈를 쓰지 않는 팀이라면 이 단계는 생략해도 됩니다.</p>
</blockquote>

<h3 id="step-3-작업-브랜치-선택-또는-생성">Step 3. 작업 브랜치 선택 또는 생성</h3>

<h4 id="새-기능-시작할-때-브랜치-새로-생성">새 기능 시작할 때 브랜치 새로 생성</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">-b</span> <span class="o">{</span>prefix<span class="o">}</span>/<span class="o">{</span>작업명<span class="o">}</span>
<span class="c"># 예: git checkout -b feat/login-page</span>
</code></pre></div></div>

<h4 id="기존-브랜치에서-계속-작업할-때">기존 브랜치에서 계속 작업할 때</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git checkout <span class="o">{</span>prefix<span class="o">}</span>/<span class="o">{</span>작업명<span class="o">}</span>
git merge origin/main
</code></pre></div></div>

<blockquote class="block-tip">
  <p>feature branch에서 최신 main을 받아 충돌을 미리 확인하는 과정입니다.</p>
</blockquote>

<h4 id="다른-사람이-push만-해둔-브랜치를-받아서-새-브랜치로-이어-작업할-때">다른 사람이 push만 해둔 브랜치를 받아서 새 브랜치로 이어 작업할 때</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git branch <span class="nt">-r</span>
git checkout <span class="nt">-b</span> <span class="o">{</span>내새브랜치명<span class="o">}</span> origin/<span class="o">{</span>다른사람브랜치명<span class="o">}</span>
</code></pre></div></div>

<p>예시:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">-b</span> feat/my-fix origin/feat/casting-factory-from-kim
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">origin/{다른사람브랜치명}</code>을 시작점으로 삼고, 내 브랜치를 따로 만듭니다.
그 다음부터는 내 브랜치에서 커밋을 쌓고 <code class="language-plaintext highlighter-rouge">git push -u origin {내새브랜치명}</code>으로 올리면 됩니다.</p>
</blockquote>

<p>브랜치 이름만 봐도 무슨 작업인지 알 수 있게 적습니다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>prefix</th>
      <th>용도</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">feat/</code></td>
      <td>새 기능 개발</td>
      <td><code class="language-plaintext highlighter-rouge">feat/login-page</code>, <code class="language-plaintext highlighter-rouge">feat/data-upload</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">fix/</code></td>
      <td>버그 수정</td>
      <td><code class="language-plaintext highlighter-rouge">fix/login-null-error</code>, <code class="language-plaintext highlighter-rouge">fix/map-render</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">hotfix/</code></td>
      <td>배포 후 긴급 수정</td>
      <td><code class="language-plaintext highlighter-rouge">hotfix/payment-crash</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">chore/</code></td>
      <td>설정·문서·패키지 등 기타</td>
      <td><code class="language-plaintext highlighter-rouge">chore/update-readme</code>, <code class="language-plaintext highlighter-rouge">chore/env-setup</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">refactor/</code></td>
      <td>기능 변경 없이 코드 구조 개선</td>
      <td><code class="language-plaintext highlighter-rouge">refactor/auth-module</code></td>
    </tr>
  </tbody>
</table>

<ul>
  <li>소문자 + 하이픈(<code class="language-plaintext highlighter-rouge">-</code>) 사용, 언더스코어·대문자 금지</li>
  <li>영어로 작성, 의미를 알 수 있도록 구체적으로</li>
  <li>너무 길면 안 됨: <code class="language-plaintext highlighter-rouge">feat/user-auth</code> ✅ / <code class="language-plaintext highlighter-rouge">feat/사용자-로그인-기능-구현</code> ❌</li>
</ul>

<h3 id="step-4-작업-확인--커밋">Step 4. 작업 확인 &amp; 커밋</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch
git status
git diff
git diff <span class="nt">--staged</span>
git diff <span class="nt">--name-only</span> <span class="nt">--diff-filter</span><span class="o">=</span>U

git add <span class="o">{</span>파일명<span class="o">}</span>
git add <span class="nb">.</span>

git commit <span class="nt">-m</span> <span class="s2">"feat: 설명"</span>
</code></pre></div></div>

<blockquote class="block-warning">
  <p>작업 단위를 작게 유지하고 commit을 자주 남길 것
<code class="language-plaintext highlighter-rouge">git add .</code>를 쓰기 전에는 의도하지 않은 파일이 같이 들어가지 않았는지 <code class="language-plaintext highlighter-rouge">git status</code>로 확인할 것</p>
</blockquote>

<p>실수로 staging에 올린 파일을 내릴 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git restore <span class="nt">--staged</span> 파일명
git restore <span class="nt">--staged</span> <span class="nb">.</span>
</code></pre></div></div>

<p>커밋을 되돌릴 때 자주 보는 명령어입니다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>명령어</th>
      <th>커밋 히스토리</th>
      <th>스테이징</th>
      <th>파일 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git reset --soft HEAD~1</code></td>
      <td>취소</td>
      <td>유지</td>
      <td>유지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git reset HEAD~1</code></td>
      <td>취소</td>
      <td>취소</td>
      <td>유지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git reset --hard HEAD~1</code></td>
      <td>취소</td>
      <td>취소</td>
      <td><strong>삭제</strong> ⚠️</td>
    </tr>
  </tbody>
</table>

<h3 id="step-5-내-로컬-브랜치-원격에-올리기">Step 5. 내 로컬 브랜치 원격에 올리기</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 처음 push할 때</span>
git push <span class="nt">-u</span> origin <span class="o">{</span>prefix<span class="o">}</span>/<span class="o">{</span>작업명<span class="o">}</span>

<span class="c"># 그 다음부터 같은 브랜치에 추가 push할 때</span>
git push
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">origin/main</code>에는 영향 없음. 내 작업 브랜치만 올라갑니다.</p>
</blockquote>

<h3 id="step-6-github에서-pr-생성">Step 6. GitHub에서 PR 생성</h3>

<p>해당 기능에 대한 모든 작업이 끝난 후 PR을 보냅니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub → Pull requests → New pull request
base: main
compare: 내가 작업한 브랜치
Create pull request
</code></pre></div></div>

<blockquote class="block-tip">
  <p>PR 제목과 설명에는 “무엇을 왜 바꿨는지”를 적습니다.
리뷰어가 코드를 보기 전에 의도를 이해할 수 있어야 합니다.</p>
</blockquote>

<h3 id="step-7-리뷰-수정사항-반영">Step 7. 리뷰 수정사항 반영</h3>

<p>PR에서 수정 요청을 받으면 같은 브랜치에서 고친 뒤 다시 commit/push합니다.
새 PR을 만들 필요는 없습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="o">{</span>prefix<span class="o">}</span>/<span class="o">{</span>기능명<span class="o">}</span>

<span class="c"># 수정 작업 후</span>
git status
git add <span class="o">{</span>수정한파일<span class="o">}</span>
git commit <span class="nt">-m</span> <span class="s2">"fix: 리뷰 반영"</span>
git push
</code></pre></div></div>

<blockquote class="block-tip">
  <p>이미 PR이 열려 있는 브랜치에 push하면 GitHub PR 내용이 자동으로 업데이트됩니다.</p>
</blockquote>

<h3 id="step-8-main-변경사항을-내-브랜치에-반영">Step 8. main 변경사항을 내 브랜치에 반영</h3>

<p>작업 중에 다른 PR이 먼저 merge되면 내 브랜치가 예전 main을 기준으로 남아 있을 수 있습니다.
그럴 때는 최신 main을 받아와서 내 브랜치에 합칩니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git checkout <span class="o">{</span>prefix<span class="o">}</span>/<span class="o">{</span>기능명<span class="o">}</span>
git merge origin/main
</code></pre></div></div>

<p>충돌이 없으면 그대로 작업을 계속하면 됩니다.
충돌이 나면 충돌 파일을 직접 고친 뒤 merge commit을 마무리합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git diff <span class="nt">--name-only</span> <span class="nt">--diff-filter</span><span class="o">=</span>U

<span class="c"># 충돌 파일 수정 후</span>
git add <span class="o">{</span>충돌해결한파일<span class="o">}</span>
git commit
git push
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">git commit</code>을 실행하면 Git이 merge commit 메시지를 자동으로 준비해 줍니다.
특별히 바꿀 내용이 없으면 저장하고 종료하면 됩니다.</p>
</blockquote>

<h3 id="step-9-pr-merge-후-내-브랜치-정리">Step 9. PR merge 후 내 브랜치 정리</h3>

<p>PR이 merge되면 작업 브랜치의 역할은 끝납니다.
GitHub에서 <code class="language-plaintext highlighter-rouge">Delete branch</code> 버튼이 보이면 원격 브랜치를 먼저 삭제할 수 있습니다.</p>

<p>로컬에서는 최신 main으로 이동한 뒤 작업 브랜치를 삭제합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout main
git fetch origin
git merge origin/main

git branch <span class="nt">-d</span> <span class="o">{</span>prefix<span class="o">}</span>/<span class="o">{</span>기능명<span class="o">}</span>
</code></pre></div></div>

<p>원격 브랜치까지 터미널에서 삭제하고 싶으면 아래처럼 합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin <span class="nt">--delete</span> <span class="o">{</span>prefix<span class="o">}</span>/<span class="o">{</span>기능명<span class="o">}</span>
</code></pre></div></div>

<p>로컬에 남아 있는 원격 브랜치 목록을 정리하려면:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch <span class="nt">--prune</span> origin
git branch <span class="nt">-a</span>
</code></pre></div></div>

<blockquote class="block-warning">
  <p><code class="language-plaintext highlighter-rouge">git branch -d</code>는 merge된 브랜치만 안전하게 삭제합니다.
아직 merge되지 않은 브랜치를 강제로 지우려면 <code class="language-plaintext highlighter-rouge">git branch -D {브랜치명}</code>을 쓰지만, 커밋을 잃을 수 있으니 PR merge 여부를 먼저 확인합니다.</p>
</blockquote>

<h2 id="conflict가-난-경우-대처법">Conflict가 난 경우 대처법</h2>

<p>충돌은 Git이 “두 사람이 같은 부분을 바꿔서 자동으로 합칠 수 없다”고 알려주는 상태입니다.
먼저 어떤 파일에서 충돌이 났는지 확인합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git diff <span class="nt">--name-only</span> <span class="nt">--diff-filter</span><span class="o">=</span>U
</code></pre></div></div>

<p>충돌 파일에는 보통 아래 표시가 생깁니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD
내 현재 브랜치의 내용
=======
합치려는 브랜치의 내용
&gt;&gt;&gt;&gt;&gt;&gt;&gt; origin/main
</code></pre></div></div>

<p>해결 순서:</p>

<ol>
  <li>충돌난 파일을 열어서 필요한 코드만 남깁니다.</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>, <code class="language-plaintext highlighter-rouge">=======</code>, <code class="language-plaintext highlighter-rouge">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> 표시를 전부 삭제합니다.</li>
  <li>팀원 작업과 겹치는 부분이면 혼자 판단하지 말고 같이 확인합니다.</li>
  <li>해결한 파일을 다시 <code class="language-plaintext highlighter-rouge">git add</code>합니다.</li>
  <li>merge 중이었다면 <code class="language-plaintext highlighter-rouge">git commit</code>으로 merge를 마무리합니다.</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="o">{</span>충돌해결한파일<span class="o">}</span>
git commit
</code></pre></div></div>

<blockquote class="block-warning">
  <p>VS Code에서 충돌을 해결할 때 <code class="language-plaintext highlighter-rouge">Accept Current</code>, <code class="language-plaintext highlighter-rouge">Accept Incoming</code>, <code class="language-plaintext highlighter-rouge">Accept Both</code>, <code class="language-plaintext highlighter-rouge">Complete Merge</code> 버튼이 보일 수 있습니다.
버튼을 누른 뒤에도 반드시 파일 내용을 직접 확인하고 <code class="language-plaintext highlighter-rouge">git status</code>로 충돌이 끝났는지 확인합니다.</p>
</blockquote>

<h3 id="작업-중-변경사항-때문에-브랜치-이동이-안-될-때">작업 중 변경사항 때문에 브랜치 이동이 안 될 때</h3>

<p>아직 커밋하기 애매한 수정이 있는데 다른 브랜치로 이동해야 하면 <code class="language-plaintext highlighter-rouge">stash</code>를 사용합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git stash push <span class="nt">-m</span> <span class="s2">"login 작업 중 임시 저장"</span>
git stash list

git checkout <span class="o">{</span>이동할브랜치<span class="o">}</span>
git stash apply <span class="s1">'stash@{0}'</span>
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">git stash apply</code>는 stash 목록을 남겨둔 채 적용합니다.
적용 후 문제가 없으면 <code class="language-plaintext highlighter-rouge">git stash drop 'stash@{0}'</code>으로 지워도 됩니다.</p>
</blockquote>

<h2 id="브랜치-간-이동이-잦을-때-worktree-만들기">브랜치 간 이동이 잦을 때 worktree 만들기</h2>

<p><code class="language-plaintext highlighter-rouge">git worktree</code>는 같은 레포의 다른 브랜치를 별도 폴더에 꺼내두는 기능입니다.
브랜치를 자주 왔다 갔다 해야 할 때 유용합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git worktree add ../<span class="o">{</span>생성할폴더이름<span class="o">}</span> <span class="o">{</span>브랜치이름<span class="o">}</span>
git worktree list
</code></pre></div></div>

<p>예시:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git worktree add ../project-login feat/login-page
git worktree add ../project-main main
</code></pre></div></div>

<h3 id="remote-branch를-가져와서-worktree를-만들고-싶을-때">remote branch를 가져와서 worktree를 만들고 싶을 때</h3>

<p>원격 브랜치를 기준으로 새 로컬 브랜치를 만들면서 worktree를 추가합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git worktree add <span class="nt">-b</span> <span class="o">{</span>새로만들로컬브랜치명<span class="o">}</span> ../<span class="o">{</span>생성할폴더이름<span class="o">}</span> origin/<span class="o">{</span>원격브랜치명<span class="o">}</span>
</code></pre></div></div>

<p>예시:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git worktree add <span class="nt">-b</span> study/gitguideline-copy ../dayelee-git-guide origin/study/gitguideline
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">-b {새로만들로컬브랜치명}</code>은 새 worktree에서 사용할 로컬 브랜치 이름입니다.
<code class="language-plaintext highlighter-rouge">../{생성할폴더이름}</code>은 worktree 폴더가 만들어질 위치이고, <code class="language-plaintext highlighter-rouge">origin/{원격브랜치명}</code>은 시작점으로 삼을 remote branch입니다.</p>
</blockquote>

<blockquote class="block-warning">
  <p>worktree는 같은 브랜치를 두 폴더에서 동시에 checkout할 수 없습니다.
이미 사용 중인 브랜치라면 새 브랜치를 만들거나 기존 worktree를 정리해야 합니다.</p>
</blockquote>

<h2 id="worktree와-branch-정리">worktree와 branch 정리</h2>

<p><code class="language-plaintext highlighter-rouge">worktree</code>와 <code class="language-plaintext highlighter-rouge">branch</code>는 역할이 다릅니다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">worktree</code>: 로컬 작업 폴더</li>
  <li><code class="language-plaintext highlighter-rouge">branch</code>: Git 안의 작업 기록</li>
</ul>

<h3 id="-c는-뭐예요"><code class="language-plaintext highlighter-rouge">-C</code>는 뭐예요?</h3>

<p><code class="language-plaintext highlighter-rouge">git -C &lt;경로&gt; ...</code>는 해당 경로에서 git 명령을 실행하라는 뜻입니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git <span class="nt">-C</span> /home/addinedu/dev_ws/SmartCastRobotics_team2 worktree list
</code></pre></div></div>

<p>위 명령은 아래와 같습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /home/addinedu/dev_ws/SmartCastRobotics_team2
git worktree list
</code></pre></div></div>

<h3 id="삭제-방법">삭제 방법</h3>

<p>worktree만 지울 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git <span class="nt">-C</span> /home/addinedu/dev_ws/SmartCastRobotics_team2 worktree remove /home/addinedu/dev_ws/SmartCastRobotics_team2-ui_db
</code></pre></div></div>

<p>branch까지 같이 지울 때:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git <span class="nt">-C</span> /home/addinedu/dev_ws/SmartCastRobotics_team2 branch <span class="nt">-d</span> <span class="nb">test</span>/ui_db_integration
</code></pre></div></div>

<p>merge되지 않은 브랜치를 강제로 지워야 하면 <code class="language-plaintext highlighter-rouge">-D</code>를 씁니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git <span class="nt">-C</span> /home/addinedu/dev_ws/SmartCastRobotics_team2 branch <span class="nt">-D</span> <span class="nb">test</span>/ui_db_integration
</code></pre></div></div>

<h3 id="언제-다르게-지우나요">언제 다르게 지우나요?</h3>

<ul>
  <li>다시 열어볼 가능성이 있으면 <code class="language-plaintext highlighter-rouge">worktree</code>만 지웁니다.</li>
  <li>merge가 끝났고 브랜치도 더 안 쓰면 <code class="language-plaintext highlighter-rouge">branch</code>까지 지웁니다.</li>
  <li>확실하지 않으면 <code class="language-plaintext highlighter-rouge">worktree</code>만 지우고 <code class="language-plaintext highlighter-rouge">branch</code>는 남깁니다.</li>
</ul>

<h3 id="같이-지워지나요">같이 지워지나요?</h3>

<p>아니요. <code class="language-plaintext highlighter-rouge">branch</code>를 삭제해도 <code class="language-plaintext highlighter-rouge">worktree</code>는 자동으로 안 없어집니다.
보통은 <code class="language-plaintext highlighter-rouge">worktree remove</code>로 폴더를 지운 뒤, 필요하면 <code class="language-plaintext highlighter-rouge">branch -d</code> 또는 <code class="language-plaintext highlighter-rouge">branch -D</code>를 따로 실행합니다.</p>

<h2 id="git-핵심-개념">Git 핵심 개념</h2>

<h3 id="세-공간의-흐름">세 공간의 흐름</h3>

<style>
.mermaid { overflow: auto; }
</style>

<pre><code class="language-mermaid">flowchart TD
    subgraph remote["☁️ 원격 (Remote / origin)"]
        E["GitHub에 저장된 공유 커밋 히스토리"]
    end
    subgraph local["💻 로컬 (Local)"]
        direction LR
        D["🗄️ stash&lt;br/&gt;임시 저장소"]
        A["📁 작업 디렉토리&lt;br/&gt;(Working Directory)"]
        B["📋 스테이징 영역&lt;br/&gt;(Staging Area)"]
        C["📜 커밋 히스토리&lt;br/&gt;(Commit History)"]
    end

    A --&gt;|"git add"| B
    B --&gt;|"git commit"| C
    B --&gt;|"git restore --staged"| A
    A --&gt;|"git stash"| D
    D --&gt;|"git stash pop"| A
    C --&gt;|"git reset --soft HEAD~1"| B
    C --&gt;|"git reset HEAD~1"| A
    C --&gt;|"git merge"| A
    C --&gt;|"git push"| E
    E --&gt;|"git fetch"| C
    E --&gt;|"git pull&lt;br/&gt;(fetch+merge)"| A
</code></pre>

<blockquote class="block-tip">
  <p>스테이징 영역은 브랜치가 여럿이어도 레포 전체에서 <strong>하나</strong>입니다.
브랜치를 전환할 때 현재 변경사항이 대상 브랜치 파일과 충돌하면 <code class="language-plaintext highlighter-rouge">checkout</code>이 거부될 수 있습니다.</p>
</blockquote>

<h3 id="브랜치가-갈라지고-합쳐지는-흐름">브랜치가 갈라지고 합쳐지는 흐름</h3>

<p>새 작업 브랜치는 <code class="language-plaintext highlighter-rouge">main</code>의 특정 시점에서 갈라져 나옵니다.
작업 브랜치에 commit을 쌓고 push한 뒤 PR이 merge되면, 그 결과가 다시 <code class="language-plaintext highlighter-rouge">main</code>에 들어갑니다.</p>

<pre><code class="language-mermaid">flowchart LR
    M1["main&lt;br/&gt;A"] --&gt; M2["main&lt;br/&gt;B"]
    M2 --&gt; M3["main&lt;br/&gt;C"]
    M3 --&gt; M4["main&lt;br/&gt;D"]

    M2 --&gt; F1["study/gitguideline&lt;br/&gt;작업 시작"]
    F1 --&gt; F2["study/gitguideline&lt;br/&gt;commit 1"]
    F2 --&gt; F3["study/gitguideline&lt;br/&gt;commit 2"]
    F3 --&gt; PR["PR&lt;br/&gt;base: main&lt;br/&gt;compare: study/gitguideline"]
    PR --&gt; M4

    M3 --&gt; G1["feat/login-page&lt;br/&gt;다른 팀원 작업"]
    G1 --&gt; G2["feat/login-page&lt;br/&gt;commit"]
    G2 --&gt; M4
</code></pre>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">git push -u origin study/gitguideline</code>은 <code class="language-plaintext highlighter-rouge">origin/study/gitguideline</code>에만 올라갑니다.
<code class="language-plaintext highlighter-rouge">origin/main</code>에 반영하려면 PR로 merge하거나, 권한이 있을 때 <code class="language-plaintext highlighter-rouge">main</code>에서 merge 후 <code class="language-plaintext highlighter-rouge">git push origin main</code>을 해야 합니다.</p>
</blockquote>

<h3 id="git-checkout의-두-가지-역할"><code class="language-plaintext highlighter-rouge">git checkout</code>의 두 가지 역할</h3>

<p><code class="language-plaintext highlighter-rouge">git checkout</code>은 크게 두 가지 용도로 쓰입니다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>용도</th>
      <th>명령어 예시</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>브랜치/커밋 전환</td>
      <td><code class="language-plaintext highlighter-rouge">git checkout feat/login</code></td>
      <td>다른 브랜치나 특정 커밋으로 이동</td>
    </tr>
    <tr>
      <td>작업 내용 되돌리기</td>
      <td><code class="language-plaintext highlighter-rouge">git checkout -- app.py</code> 또는 <code class="language-plaintext highlighter-rouge">git checkout .</code></td>
      <td>Working Directory의 수정 내용을 마지막 커밋 상태로 복원</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 브랜치 전환</span>
git checkout feat/login

<span class="c"># 새 브랜치 생성 + 전환</span>
git checkout <span class="nt">-b</span> feat/login

<span class="c"># 특정 파일의 수정 내용 삭제</span>
git checkout <span class="nt">--</span> app.py

<span class="c"># 현재 폴더 아래의 tracked file 수정 내용 전체 삭제</span>
git checkout <span class="nb">.</span>
</code></pre></div></div>

<blockquote class="block-warning">
  <p><code class="language-plaintext highlighter-rouge">git checkout -- 파일명</code> 또는 <code class="language-plaintext highlighter-rouge">git checkout .</code>은 commit하지 않은 수정 내용을 되돌립니다.
실행 전에는 반드시 <code class="language-plaintext highlighter-rouge">git status</code>와 <code class="language-plaintext highlighter-rouge">git diff</code>로 없어져도 되는 변경인지 확인합니다.</p>
</blockquote>

<h3 id="동시-작업">동시 작업</h3>

<p><strong>한 사람이 여러 기능을 작업할 때</strong></p>

<p>커밋하기엔 애매한데 브랜치를 바꿔야 할 때 <code class="language-plaintext highlighter-rouge">git stash</code>를 사용합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash
git checkout feat/B-기능
<span class="c"># B 기능 작업 후 커밋 ...</span>
git checkout feat/A-기능
git stash pop
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">git stash pop</code>은 stash를 복원하면서 목록에서 제거합니다.
충돌이 나면 자동으로 사라지지 않을 수 있으니 <code class="language-plaintext highlighter-rouge">git status</code>로 확인합니다.</p>
</blockquote>

<p><strong>여러 사람이 동시에 작업할 때</strong></p>

<p>각자 다른 브랜치에서 작업하고, 주기적으로 <code class="language-plaintext highlighter-rouge">git merge origin/main</code>으로 최신 main을 받아옵니다.</p>

<hr />

<h2 id="자주-헷갈리는-상황">자주 헷갈리는 상황</h2>

<h3 id="현재-브랜치가-어디서-왔는지-확인하고-싶을-때">현재 브랜치가 어디서 왔는지 확인하고 싶을 때</h3>

<p>현재 내가 어느 브랜치에 있고, 어떤 원격 브랜치를 추적하는지 확인합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-vv</span>
</code></pre></div></div>

<p>예시 출력:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>* feat/login-page  a1b2c3d [origin/feat/login-page] feat: 로그인 페이지 추가
  main             e4f5g6h [origin/main] Merge pull request #12
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">[origin/feat/login-page]</code>가 현재 로컬 브랜치가 추적하는 원격 브랜치입니다.
upstream이 안 보이면 아직 <code class="language-plaintext highlighter-rouge">git push -u origin {브랜치명}</code>을 하지 않았거나 추적 브랜치가 설정되지 않은 상태입니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status <span class="nt">-sb</span>
git remote <span class="nt">-v</span>
git config <span class="nt">--get</span> branch.<span class="si">$(</span>git branch <span class="nt">--show-current</span><span class="si">)</span>.remote
git config <span class="nt">--get</span> branch.<span class="si">$(</span>git branch <span class="nt">--show-current</span><span class="si">)</span>.merge
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">git status -sb</code>는 현재 브랜치와 원격 추적 상태를 짧게 보여줍니다.
<code class="language-plaintext highlighter-rouge">ahead</code>는 내 로컬에만 있는 커밋, <code class="language-plaintext highlighter-rouge">behind</code>는 원격에만 있는 커밋입니다.</p>
</blockquote>

<h3 id="이-브랜치에서-지금까지-뭘-했는지-확인하고-싶을-때">이 브랜치에서 지금까지 뭘 했는지 확인하고 싶을 때</h3>

<p>내 브랜치가 <code class="language-plaintext highlighter-rouge">main</code>과 비교해서 어떤 커밋을 추가했는지 봅니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> origin/main..HEAD
</code></pre></div></div>

<p>파일 변경 목록만 보고 싶으면:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff <span class="nt">--name-status</span> origin/main...HEAD
</code></pre></div></div>

<p>실제 코드 변경 내용을 보고 싶으면:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff origin/main...HEAD
</code></pre></div></div>

<p>커밋 그래프를 한눈에 보고 싶으면:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">origin/main..HEAD</code>는 “main에는 없고 내 브랜치에만 있는 커밋”을 봅니다.
<code class="language-plaintext highlighter-rouge">origin/main...HEAD</code>는 브랜치가 갈라진 지점부터 현재까지의 변경사항을 비교할 때 자주 씁니다.</p>
</blockquote>

<h3 id="내가-브랜치에서-어떤-이동을-했는지-확인하고-싶을-때">내가 브랜치에서 어떤 이동을 했는지 확인하고 싶을 때</h3>

<p><code class="language-plaintext highlighter-rouge">reflog</code>는 내 로컬에서 HEAD가 어디를 지나왔는지 보여줍니다.
브랜치를 언제 만들었는지, checkout을 어디로 했는지, reset을 했는지 추적할 때 유용합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reflog
git reflog <span class="nt">--date</span><span class="o">=</span><span class="nb">local</span>
</code></pre></div></div>

<p>예시:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a1b2c3d HEAD@{0}: commit: feat: 로그인 페이지 추가
e4f5g6h HEAD@{1}: checkout: moving from main to feat/login-page
</code></pre></div></div>

<blockquote class="block-warning">
  <p><code class="language-plaintext highlighter-rouge">reflog</code>는 GitHub 기록이 아니라 내 컴퓨터 안의 이동 기록입니다.
다른 팀원의 컴퓨터에서는 다르게 보일 수 있습니다.</p>
</blockquote>

<h3 id="이미-원격에-있는-브랜치로-이동하고-싶을-때">이미 원격에 있는 브랜치로 이동하고 싶을 때</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git checkout <span class="o">{</span>브랜치명<span class="o">}</span>
</code></pre></div></div>

<p>예시:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout study/gitguideline
</code></pre></div></div>

<p>Git이 자동으로 <code class="language-plaintext highlighter-rouge">origin/study/gitguideline</code>을 추적하는 로컬 브랜치를 만들어줄 수 있습니다.
명시적으로 쓰고 싶으면 아래처럼 작성합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">-b</span> study/gitguideline origin/study/gitguideline
</code></pre></div></div>

<h3 id="커밋하기-전-수정-내용을-잠깐-치워야-할-때">커밋하기 전 수정 내용을 잠깐 치워야 할 때</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git stash push <span class="nt">-m</span> <span class="s2">"작업 중 임시 저장"</span>
git stash list
git stash apply <span class="s1">'stash@{0}'</span>
</code></pre></div></div>

<blockquote class="block-tip">
  <p><code class="language-plaintext highlighter-rouge">apply</code>는 stash 목록을 남겨두고, <code class="language-plaintext highlighter-rouge">pop</code>은 적용하면서 목록에서 제거합니다.
처음에는 <code class="language-plaintext highlighter-rouge">apply</code>가 더 안전합니다.</p>
</blockquote>

<h3 id="worktree를-정리하고-싶을-때">worktree를 정리하고 싶을 때</h3>

<p>worktree 폴더를 더 이상 쓰지 않으면 아래처럼 제거합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git worktree list
git worktree remove ../<span class="o">{</span>worktree폴더명<span class="o">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="guide" /><category term="git" /><category term="team" /><summary type="html"><![CDATA[팀원이 매일 쓰는 Git 흐름]]></summary></entry><entry><title type="html">Pydantic naming convention guideline</title><link href="https://daye-lee18.github.io/blog/2026/pydantic_naming_convention/" rel="alternate" type="text/html" title="Pydantic naming convention guideline" /><published>2026-04-21T00:00:00+00:00</published><updated>2026-04-21T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2026/pydantic_naming_convention</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2026/pydantic_naming_convention/"><![CDATA[<h2 id="1-pydantic-일반-설명">1. Pydantic 일반 설명</h2>

<p>Pydantic은 Python에서 <strong>데이터의 타입과 구조를 검증</strong>하는 라이브러리다.
클래스를 정의하면 그 클래스의 객체(object)로 데이터를 주고받고, 타입이 맞지 않으면 자동으로 에러를 낸다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pydantic</span> <span class="kn">import</span> <span class="n">BaseModel</span>

<span class="k">class</span> <span class="nc">CreateOrdInput</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">user_id</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">quantity</span><span class="p">:</span> <span class="nb">int</span>

<span class="n">data</span> <span class="o">=</span> <span class="nc">CreateOrdInput</span><span class="p">(</span><span class="n">user_id</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">quantity</span><span class="o">=</span><span class="mi">3</span><span class="p">)</span>      <span class="c1"># OK
</span><span class="n">data</span> <span class="o">=</span> <span class="nc">CreateOrdInput</span><span class="p">(</span><span class="n">user_id</span><span class="o">=</span><span class="sh">"</span><span class="s">abc</span><span class="sh">"</span><span class="p">,</span> <span class="n">quantity</span><span class="o">=</span><span class="mi">3</span><span class="p">)</span>  <span class="c1"># ValidationError 자동 발생
</span></code></pre></div></div>

<h3 id="1-1-naming-style-비교">1-1. Naming Style 비교</h3>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>이름</th>
      <th>규칙</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>PascalCase</strong></td>
      <td>모든 단어 첫 글자 대문자</td>
      <td><code class="language-plaintext highlighter-rouge">CreateTaskInput</code></td>
    </tr>
    <tr>
      <td><strong>camelCase</strong></td>
      <td>첫 단어만 소문자, 나머지 대문자</td>
      <td><code class="language-plaintext highlighter-rouge">createTaskInput</code></td>
    </tr>
    <tr>
      <td><strong>snake_case</strong></td>
      <td>소문자 + 언더바</td>
      <td><code class="language-plaintext highlighter-rouge">create_task_input</code></td>
    </tr>
    <tr>
      <td><strong>kebab-case</strong></td>
      <td>소문자 + 하이픈</td>
      <td><code class="language-plaintext highlighter-rouge">create-task-input</code></td>
    </tr>
    <tr>
      <td><strong>SCREAMING_SNAKE_CASE</strong></td>
      <td>전체 대문자 + 언더바</td>
      <td><code class="language-plaintext highlighter-rouge">AMR_COORDS</code></td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">_SCREAMING_SNAKE_CASE</code></strong></td>
      <td>앞에 <code class="language-plaintext highlighter-rouge">_</code> 붙인 SCREAMING_SNAKE_CASE</td>
      <td><code class="language-plaintext highlighter-rouge">_AMR_COORDS</code></td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">_snake_case</code></strong></td>
      <td>앞에 <code class="language-plaintext highlighter-rouge">_</code> 붙인 snake_case</td>
      <td><code class="language-plaintext highlighter-rouge">_run()</code></td>
    </tr>
  </tbody>
</table>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>create task input  →  (PascalCase)           CreateTaskInput
                   →  (camelCase)            createTaskInput
                   →  (snake_case)           create_task_input
                   →  (kebab-case)           create-task-input
AMR coordinates   →  (SCREAMING_SNAKE_CASE) AMR_COORDS
                   →  (module-private)       _AMR_COORDS
</code></pre></div></div>

<p>Python <a href="https://peps.python.org/pep-0008/">PEP 8</a> 표준:</p>
<ul>
  <li><strong>클래스명</strong> → PascalCase (<code class="language-plaintext highlighter-rouge">class CreateTaskInput</code>)</li>
  <li><strong>함수/변수명</strong> → snake_case (<code class="language-plaintext highlighter-rouge">def create_task()</code>)</li>
  <li><strong>상수</strong> → SCREAMING_SNAKE_CASE (<code class="language-plaintext highlighter-rouge">MAX_RETRY = 3</code>, <code class="language-plaintext highlighter-rouge">AMR_COORDS = {...}</code>)</li>
</ul>

<p><strong><code class="language-plaintext highlighter-rouge">_</code> 앞에 붙이는 의미</strong> — “이 파일 안에서만 써라, 외부에서 <code class="language-plaintext highlighter-rouge">import</code>하지 마라”는 신호다. 클래스·함수·변수·상수 모두에 붙일 수 있다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>종류</th>
      <th>공개</th>
      <th>모듈 내부 전용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스</td>
      <td><code class="language-plaintext highlighter-rouge">AmrCoord</code></td>
      <td><code class="language-plaintext highlighter-rouge">_AmrCoord</code></td>
    </tr>
    <tr>
      <td>상수</td>
      <td><code class="language-plaintext highlighter-rouge">AMR_COORDS</code></td>
      <td><code class="language-plaintext highlighter-rouge">_AMR_COORDS</code></td>
    </tr>
    <tr>
      <td>함수</td>
      <td><code class="language-plaintext highlighter-rouge">run()</code></td>
      <td><code class="language-plaintext highlighter-rouge">_run()</code></td>
    </tr>
  </tbody>
</table>

<h3 id="1-2-모델-클래스-suffix-규칙">1-2. 모델 클래스 Suffix 규칙</h3>

<p>데이터의 역할에 따라 접미사(Suffix)를 붙여 구분한다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>Suffix</th>
      <th>용도</th>
      <th>패턴</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Input</code></td>
      <td>함수에 넣는 입력값</td>
      <td><code class="language-plaintext highlighter-rouge">[동사][명사]Input</code></td>
      <td><code class="language-plaintext highlighter-rouge">CreateOrdInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Record</code></td>
      <td>DB 행(row) 1:1 표현</td>
      <td><code class="language-plaintext highlighter-rouge">[테이블명]Record</code></td>
      <td><code class="language-plaintext highlighter-rouge">OrdRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Result</code></td>
      <td>연산 결과 (DB 아님)</td>
      <td><code class="language-plaintext highlighter-rouge">[동사][명사]Result</code></td>
      <td><code class="language-plaintext highlighter-rouge">AllocateTaskResult</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Event</code></td>
      <td>이벤트/로그 페이로드</td>
      <td><code class="language-plaintext highlighter-rouge">[명사]Event</code></td>
      <td><code class="language-plaintext highlighter-rouge">TransErrorEvent</code></td>
    </tr>
  </tbody>
</table>

<h3 id="1-3-필드명--snake_case">1-3. 필드명 — <code class="language-plaintext highlighter-rouge">snake_case</code></h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">CreateOrdInput</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">user_id</span><span class="p">:</span> <span class="nb">int</span>           <span class="c1"># snake_case
</span>    <span class="n">prod_opt_id</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">quantity</span><span class="p">:</span> <span class="nb">int</span>
</code></pre></div></div>

<h3 id="1-4-유효성-검사--데코레이터">1-4. 유효성 검사 &amp; 데코레이터</h3>

<p><code class="language-plaintext highlighter-rouge">BaseModel</code> 안에 선언된 변수들을 <strong>field(필드)</strong>라고 한다. 유효성 검사 방법은 복잡도에 따라 세 가지로 나뉜다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>방법</th>
      <th>코드</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>타입 힌트만</strong></td>
      <td><code class="language-plaintext highlighter-rouge">quantity: int</code></td>
      <td>타입만 맞으면 충분할 때</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">Field()</code></strong></td>
      <td><code class="language-plaintext highlighter-rouge">Field(gt=0, max_length=50)</code></td>
      <td>범위·길이 같은 단순 조건</td>
    </tr>
    <tr>
      <td><strong><code class="language-plaintext highlighter-rouge">@field_validator</code></strong></td>
      <td>데코레이터 함수</td>
      <td>위 두 가지로 표현 못하는 복잡한 로직</td>
    </tr>
  </tbody>
</table>

<blockquote class="block-tip">
  <p>단순 범위·길이 → <code class="language-plaintext highlighter-rouge">Field()</code> / 복잡한 로직·변환 → <code class="language-plaintext highlighter-rouge">@field_validator</code> / 여러 필드를 같이 봐야 하면 → <code class="language-plaintext highlighter-rouge">@model_validator</code></p>
</blockquote>

<hr />

<details><summary>@field_validator — 특정 필드 검증</summary>
<p>데코레이터의 문자열 인자가 <strong>어떤 필드에 연결된 검증 함수인지</strong> Pydantic에 알려준다. 해당 필드 값이 들어올 때 자동으로 호출되며, 여러 필드에 동시에 적용할 수도 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@field_validator</span><span class="p">(</span><span class="sh">"</span><span class="s">quantity</span><span class="sh">"</span><span class="p">)</span>           <span class="c1"># quantity 필드 전용
</span><span class="nd">@field_validator</span><span class="p">(</span><span class="sh">"</span><span class="s">user_id</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">quantity</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># 두 필드 모두 이 함수로 검증
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@classmethod</code>가 같이 붙는 이유: 객체가 만들어지기 <strong>전</strong>에 값을 검사하므로 <code class="language-plaintext highlighter-rouge">self</code>가 없다. Pydantic v2 스펙상 <code class="language-plaintext highlighter-rouge">cls</code>를 받는 classmethod로 선언해야 한다. 실제로 <code class="language-plaintext highlighter-rouge">cls</code>를 쓸 일은 거의 없다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">CreateOrdInput</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">quantity</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">phone</span><span class="p">:</span> <span class="nb">str</span>

    <span class="nd">@field_validator</span><span class="p">(</span><span class="sh">"</span><span class="s">quantity</span><span class="sh">"</span><span class="p">)</span>
    <span class="nd">@classmethod</span>
    <span class="k">def</span> <span class="nf">validate_quantity</span><span class="p">(</span><span class="n">cls</span><span class="p">,</span> <span class="n">v</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">v</span> <span class="o">%</span> <span class="mi">10</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">:</span>                       <span class="c1"># Field()로 표현 불가한 비즈니스 로직
</span>            <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sh">"</span><span class="s">10의 배수만 허용</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">v</span>

    <span class="nd">@field_validator</span><span class="p">(</span><span class="sh">"</span><span class="s">phone</span><span class="sh">"</span><span class="p">)</span>
    <span class="nd">@classmethod</span>
    <span class="k">def</span> <span class="nf">validate_phone</span><span class="p">(</span><span class="n">cls</span><span class="p">,</span> <span class="n">v</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">re</span><span class="p">.</span><span class="nf">match</span><span class="p">(</span><span class="sa">r</span><span class="sh">"</span><span class="s">^010-\d{4}-\d{4}$</span><span class="sh">"</span><span class="p">,</span> <span class="n">v</span><span class="p">):</span>
            <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sh">"</span><span class="s">전화번호 형식 오류</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">v</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">"</span><span class="s">-</span><span class="sh">"</span><span class="p">,</span> <span class="sh">""</span><span class="p">)</span>             <span class="c1"># 검증 후 값 변환도 가능
</span>
    <span class="nd">@field_validator</span><span class="p">(</span><span class="sh">"</span><span class="s">quantity</span><span class="sh">"</span><span class="p">)</span>
    <span class="nd">@classmethod</span>
    <span class="k">def</span> <span class="nf">check_db</span><span class="p">(</span><span class="n">cls</span><span class="p">,</span> <span class="n">v</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">db</span><span class="p">.</span><span class="nf">exists</span><span class="p">(</span><span class="n">v</span><span class="p">):</span>                  <span class="c1"># DB 조회도 가능
</span>            <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sh">"</span><span class="s">존재하지 않는 값</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">v</span>
</code></pre></div></div>

<p>실행 흐름:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CreateOrdInput(quantity=-1)
      ↓
Pydantic: "quantity 필드 들어왔다"
      ↓
@field_validator("quantity") 찾아서 실행
      ↓
validate_quantity(cls, v=-1) 호출
      ↓
raise ValueError → ValidationError 발생
</code></pre></div></div>

</details>

<details><summary>Field() — 함수 없이 간단한 제약</summary>
<p>범위·길이처럼 단순한 조건은 <code class="language-plaintext highlighter-rouge">@field_validator</code> 없이 한 줄로 처리할 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">CreateOrdInput</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">quantity</span><span class="p">:</span>  <span class="nb">int</span> <span class="o">=</span> <span class="nc">Field</span><span class="p">(</span><span class="n">gt</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>                       <span class="c1"># 0 초과
</span>    <span class="n">user_id</span><span class="p">:</span>   <span class="nb">int</span> <span class="o">=</span> <span class="nc">Field</span><span class="p">(</span><span class="n">ge</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>                       <span class="c1"># 1 이상
</span>    <span class="n">prod_nm</span><span class="p">:</span>   <span class="nb">str</span> <span class="o">=</span> <span class="nc">Field</span><span class="p">(</span><span class="n">min_length</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">max_length</span><span class="o">=</span><span class="mi">50</span><span class="p">)</span>
</code></pre></div></div>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>옵션</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">gt</code></td>
      <td>초과 (greater than)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ge</code></td>
      <td>이상 (greater or equal)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">lt</code></td>
      <td>미만</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">le</code></td>
      <td>이하</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">min_length</code> / <code class="language-plaintext highlighter-rouge">max_length</code></td>
      <td>문자열 길이</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pattern</code></td>
      <td>정규식</td>
    </tr>
  </tbody>
</table>

</details>

<details><summary>@model_validator — 필드 간 관계 검증</summary>
<p>한 필드만으로는 판단할 수 없고, <strong>두 필드 이상을 같이 봐야</strong> 할 때 사용한다. <code class="language-plaintext highlighter-rouge">mode</code> 파라미터로 실행 시점을 결정한다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th> </th>
      <th><code class="language-plaintext highlighter-rouge">mode="before"</code></th>
      <th><code class="language-plaintext highlighter-rouge">mode="after"</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>실행 시점</td>
      <td>필드 변환·검증 전</td>
      <td>모든 필드 검증 완료 후</td>
    </tr>
    <tr>
      <td>받는 것</td>
      <td>raw <code class="language-plaintext highlighter-rouge">dict</code></td>
      <td>완성된 모델 인스턴스 (<code class="language-plaintext highlighter-rouge">self</code>)</td>
    </tr>
    <tr>
      <td>반환</td>
      <td><code class="language-plaintext highlighter-rouge">dict</code></td>
      <td><code class="language-plaintext highlighter-rouge">self</code></td>
    </tr>
    <tr>
      <td>언제 씀</td>
      <td>데이터 전처리, 필드 이름 변환</td>
      <td>필드 간 관계 검증</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@classmethod</code> 필요?</td>
      <td>✅</td>
      <td>❌</td>
    </tr>
  </tbody>
</table>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ShipmentOrdInput</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">quantity</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">available_stock</span><span class="p">:</span> <span class="nb">int</span>

    <span class="nd">@model_validator</span><span class="p">(</span><span class="n">mode</span><span class="o">=</span><span class="sh">"</span><span class="s">after</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">def</span> <span class="nf">validate_stock</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="sh">"</span><span class="s">ShipmentOrdInput</span><span class="sh">"</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">quantity</span> <span class="o">&gt;</span> <span class="n">self</span><span class="p">.</span><span class="n">available_stock</span><span class="p">:</span>
            <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sh">"</span><span class="s">수량이 재고보다 많습니다</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span>

<span class="nc">ShipmentOrdInput</span><span class="p">(</span><span class="n">quantity</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">available_stock</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>   <span class="c1"># ValidationError
</span><span class="nc">ShipmentOrdInput</span><span class="p">(</span><span class="n">quantity</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span>  <span class="n">available_stock</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>   <span class="c1"># OK
</span></code></pre></div></div>

</details>

<details><summary>@computed_field — DB에 없는 계산값 노출</summary>
<p>DB에 저장하진 않지만 응답에 포함시키고 싶은 파생 값이 있을 때 사용한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">OrdRecord</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">unit_price</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">quantity</span><span class="p">:</span>   <span class="nb">int</span>

    <span class="nd">@computed_field</span>
    <span class="nd">@property</span>
    <span class="k">def</span> <span class="nf">total_price</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">unit_price</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">quantity</span>

<span class="n">record</span> <span class="o">=</span> <span class="nc">OrdRecord</span><span class="p">(</span><span class="n">unit_price</span><span class="o">=</span><span class="mi">1000</span><span class="p">,</span> <span class="n">quantity</span><span class="o">=</span><span class="mi">3</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">record</span><span class="p">.</span><span class="n">total_price</span><span class="p">)</span>   <span class="c1"># 3000
</span><span class="nf">print</span><span class="p">(</span><span class="n">record</span><span class="p">.</span><span class="nf">model_dump</span><span class="p">())</span>  <span class="c1"># {'unit_price': 1000, 'quantity': 3, 'total_price': 3000}
</span></code></pre></div></div>

</details>

<details><summary>@field_serializer — JSON 출력 형식 커스텀</summary>
<p>DB에서 꺼낸 값과 API 응답으로 내보낼 형식이 다를 때 (특히 날짜·enum) 사용한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">OrdRecord</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">created_at</span><span class="p">:</span> <span class="n">datetime</span>

    <span class="nd">@field_serializer</span><span class="p">(</span><span class="sh">"</span><span class="s">created_at</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">def</span> <span class="nf">serialize_created_at</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">v</span><span class="p">:</span> <span class="n">datetime</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">v</span><span class="p">.</span><span class="nf">strftime</span><span class="p">(</span><span class="sh">"</span><span class="s">%Y-%m-%d %H:%M</span><span class="sh">"</span><span class="p">)</span>

<span class="n">record</span> <span class="o">=</span> <span class="nc">OrdRecord</span><span class="p">(</span><span class="n">created_at</span><span class="o">=</span><span class="nf">datetime</span><span class="p">(</span><span class="mi">2026</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">28</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span>
<span class="nf">print</span><span class="p">(</span><span class="n">record</span><span class="p">.</span><span class="nf">model_dump</span><span class="p">())</span>
<span class="c1"># {'created_at': '2026-04-28 09:00'}   ← datetime 객체가 아닌 문자열로 출력
</span></code></pre></div></div>

</details>

<details><summary>한눈에 비교</summary>
<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>데코레이터</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@field_validator</code></td>
      <td>특정 필드 하나 (또는 여러 개) 검증</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@model_validator</code></td>
      <td>필드 간 관계 검증</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Field()</code></td>
      <td>간단한 범위·길이 제약 (함수 불필요)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@computed_field</code></td>
      <td>DB에 없는 계산값 노출</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@field_serializer</code></td>
      <td>JSON 출력 형식 커스텀</td>
    </tr>
  </tbody>
</table>

</details>

<h3 id="1-5-model_config-pydantic-v2">1-5. model_config (Pydantic v2)</h3>

<p><code class="language-plaintext highlighter-rouge">model_config</code>는 Pydantic 모델의 동작 방식을 설정하는 클래스 변수다. <code class="language-plaintext highlighter-rouge">ConfigDict</code>로 옵션을 넘긴다.</p>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>옵션</th>
      <th>언제 쓰나</th>
      <th>주로 붙는 suffix</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">from_attributes=True</code></td>
      <td>DB ORM 객체 → Pydantic 변환</td>
      <td><code class="language-plaintext highlighter-rouge">Record</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">frozen=True</code></td>
      <td>생성 후 값 변경 불가 (불변 설정값)</td>
      <td>좌표·설정 클래스</td>
    </tr>
  </tbody>
</table>

<h4 id="from_attributestrue--db-행을-pydantic으로-변환"><code class="language-plaintext highlighter-rouge">from_attributes=True</code> — DB 행을 Pydantic으로 변환</h4>

<p>Pydantic은 기본적으로 <code class="language-plaintext highlighter-rouge">dict</code>를 입력으로 받는다. 그런데 DB에서 꺼낸 ORM 객체는 <code class="language-plaintext highlighter-rouge">dict</code>가 아니라 <strong>속성(attribute)</strong>을 가진 객체다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># DB에서 꺼낸 ORM 객체 (dict가 아님)
</span><span class="n">db_row</span><span class="p">.</span><span class="n">user_id</span>    <span class="c1"># attribute 접근
</span><span class="n">db_row</span><span class="p">.</span><span class="n">quantity</span>

<span class="c1"># 기본 상태 — dict만 받으므로 ORM 객체를 넘기면 에러
</span><span class="nc">OrdRecord</span><span class="p">(</span><span class="n">user_id</span><span class="o">=</span><span class="n">db_row</span><span class="p">.</span><span class="n">user_id</span><span class="p">,</span> <span class="n">quantity</span><span class="o">=</span><span class="n">db_row</span><span class="p">.</span><span class="n">quantity</span><span class="p">)</span>  <span class="c1"># 이렇게 일일이 풀어야 함
</span>
<span class="c1"># from_attributes=True 설정 후 — ORM 객체를 바로 넘길 수 있음
</span><span class="k">class</span> <span class="nc">OrdRecord</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">model_config</span> <span class="o">=</span> <span class="nc">ConfigDict</span><span class="p">(</span><span class="n">from_attributes</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="n">user_id</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">quantity</span><span class="p">:</span> <span class="nb">int</span>

<span class="n">OrdRecord</span><span class="p">.</span><span class="nf">model_validate</span><span class="p">(</span><span class="n">db_row</span><span class="p">)</span>  <span class="c1"># ORM 객체를 바로 변환
</span></code></pre></div></div>

<p>즉, DB → Pydantic 변환을 깔끔하게 하기 위한 설정이다. <code class="language-plaintext highlighter-rouge">Record</code> suffix가 붙은 클래스에 주로 붙는다.</p>

<h4 id="frozentrue--불변-설정값-로봇-좌표-등"><code class="language-plaintext highlighter-rouge">frozen=True</code> — 불변 설정값 (로봇 좌표 등)</h4>

<p>로봇 좌표처럼 런타임에 절대 바뀌면 안 되는 설정값에 쓴다. 인스턴스 생성 후 필드를 수정하려 하면 에러를 낸다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">AmrCoord</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">model_config</span> <span class="o">=</span> <span class="nc">ConfigDict</span><span class="p">(</span><span class="n">frozen</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

    <span class="n">x</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">y</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">theta</span><span class="p">:</span> <span class="nb">float</span>


<span class="k">class</span> <span class="nc">ArmPose</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">model_config</span> <span class="o">=</span> <span class="nc">ConfigDict</span><span class="p">(</span><span class="n">frozen</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

    <span class="n">x</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">y</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">z</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">roll</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">pitch</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">yaw</span><span class="p">:</span> <span class="nb">float</span>


<span class="n">_AMR_COORDS</span><span class="p">:</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">AmrCoord</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">MV_SRC</span><span class="sh">"</span><span class="p">:</span>      <span class="nc">AmrCoord</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.90</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="mf">0.30</span><span class="p">,</span> <span class="n">theta</span><span class="o">=</span><span class="mf">0.00</span><span class="p">),</span>
    <span class="sh">"</span><span class="s">MV_DEST</span><span class="sh">"</span><span class="p">:</span>     <span class="nc">AmrCoord</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.50</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="mf">1.00</span><span class="p">,</span> <span class="n">theta</span><span class="o">=</span><span class="mf">1.57</span><span class="p">),</span>
    <span class="sh">"</span><span class="s">MV_SHIP</span><span class="sh">"</span><span class="p">:</span>     <span class="nc">AmrCoord</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.90</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="mf">1.70</span><span class="p">,</span> <span class="n">theta</span><span class="o">=</span><span class="mf">0.00</span><span class="p">),</span>
    <span class="sh">"</span><span class="s">MV_CHG</span><span class="sh">"</span><span class="p">:</span>      <span class="nc">AmrCoord</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.10</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="mf">0.10</span><span class="p">,</span> <span class="n">theta</span><span class="o">=</span><span class="mf">0.00</span><span class="p">),</span>
    <span class="sh">"</span><span class="s">RETURN_IDLE</span><span class="sh">"</span><span class="p">:</span> <span class="nc">AmrCoord</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.10</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="mf">0.10</span><span class="p">,</span> <span class="n">theta</span><span class="o">=</span><span class="mf">0.00</span><span class="p">),</span>
<span class="p">}</span>

<span class="n">coord</span> <span class="o">=</span> <span class="n">_AMR_COORDS</span><span class="p">[</span><span class="sh">"</span><span class="s">MV_SRC</span><span class="sh">"</span><span class="p">]</span>
<span class="n">coord</span><span class="p">.</span><span class="n">x</span> <span class="o">=</span> <span class="mf">1.0</span>   <span class="c1"># TypeError: frozen — 수정 불가
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">_AMR_COORDS</code>처럼 앞에 <code class="language-plaintext highlighter-rouge">_</code>를 붙인 이유는 이 파일 밖에서 직접 import하지 말라는 신호다. 일반 <code class="language-plaintext highlighter-rouge">dict[str, tuple]</code>로 쓰던 것과 비교하면:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 기존 tuple 방식 — 인덱스로만 접근
</span><span class="n">coord</span> <span class="o">=</span> <span class="n">_AMR_COORDS</span><span class="p">[</span><span class="sh">"</span><span class="s">MV_SRC</span><span class="sh">"</span><span class="p">]</span>   <span class="c1"># (0.90, 0.30, 0.00)
</span><span class="n">coord</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>   <span class="c1"># x?  y?  헷갈림
</span>
<span class="c1"># Pydantic frozen 방식 — 이름으로 접근
</span><span class="n">coord</span><span class="p">.</span><span class="n">x</span>      <span class="c1"># 0.90
</span><span class="n">coord</span><span class="p">.</span><span class="n">y</span>      <span class="c1"># 0.30
</span><span class="n">coord</span><span class="p">.</span><span class="n">theta</span>  <span class="c1"># 0.00
</span></code></pre></div></div>

<hr />

<p><br /></p>

<h2 id="2-smartcast-robotics-project--테이블-종류-요약">2. SmartCast Robotics Project — 테이블 종류 요약</h2>

<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>테이블 종류</th>
      <th>언제 쓰는가</th>
      <th>Pydantic Suffix</th>
      <th>해당 테이블</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Master</strong></td>
      <td>기준·설정 데이터. 잘 안 변함</td>
      <td><code class="language-plaintext highlighter-rouge">Record</code></td>
      <td><code class="language-plaintext highlighter-rouge">user_account</code>, <code class="language-plaintext highlighter-rouge">category</code>, <code class="language-plaintext highlighter-rouge">product</code>, <code class="language-plaintext highlighter-rouge">product_option</code>, <code class="language-plaintext highlighter-rouge">pp_options</code>, <code class="language-plaintext highlighter-rouge">res</code>, <code class="language-plaintext highlighter-rouge">equip_load_spec</code>, <code class="language-plaintext highlighter-rouge">zone</code>, <code class="language-plaintext highlighter-rouge">equip</code>, <code class="language-plaintext highlighter-rouge">trans</code>, <code class="language-plaintext highlighter-rouge">trans_task_bat_threshold</code></td>
    </tr>
    <tr>
      <td><strong>Operational (txn)</strong></td>
      <td>작업 발생·진행 기록</td>
      <td>생성 입력 → <code class="language-plaintext highlighter-rouge">Input</code> / DB 조회 → <code class="language-plaintext highlighter-rouge">Record</code></td>
      <td><code class="language-plaintext highlighter-rouge">ord</code>, <code class="language-plaintext highlighter-rouge">ord_detail</code>, <code class="language-plaintext highlighter-rouge">ord_pp_map</code>, <code class="language-plaintext highlighter-rouge">ord_txn</code>, <code class="language-plaintext highlighter-rouge">equip_task_txn</code>, <code class="language-plaintext highlighter-rouge">trans_task_txn</code>, <code class="language-plaintext highlighter-rouge">insp_task_txn</code>, <code class="language-plaintext highlighter-rouge">pp_task_txn</code></td>
    </tr>
    <tr>
      <td><strong>State (stat)</strong></td>
      <td>현재 상태 스냅샷. 빠른 조회</td>
      <td><code class="language-plaintext highlighter-rouge">Record</code></td>
      <td><code class="language-plaintext highlighter-rouge">item_stat</code>, <code class="language-plaintext highlighter-rouge">ord_stat</code>, <code class="language-plaintext highlighter-rouge">equip_stat</code>, <code class="language-plaintext highlighter-rouge">trans_stat</code>, <code class="language-plaintext highlighter-rouge">chg_loc_stat</code>, <code class="language-plaintext highlighter-rouge">strg_loc_stat</code>, <code class="language-plaintext highlighter-rouge">ship_loc_stat</code>, <code class="language-plaintext highlighter-rouge">pattern_stat</code>, <code class="language-plaintext highlighter-rouge">alerts_stat</code></td>
    </tr>
    <tr>
      <td><strong>Log</strong></td>
      <td>행동/이벤트/데이터/에러 기록</td>
      <td>행동·이벤트 → <code class="language-plaintext highlighter-rouge">Event</code> / 데이터·에러 → <code class="language-plaintext highlighter-rouge">Record</code></td>
      <td><code class="language-plaintext highlighter-rouge">ord_log</code>, <code class="language-plaintext highlighter-rouge">log_action_user</code>, <code class="language-plaintext highlighter-rouge">log_action_operator_handoff_acks</code>, <code class="language-plaintext highlighter-rouge">log_action_operator_rfid_scan</code>, <code class="language-plaintext highlighter-rouge">log_action_admin</code>, <code class="language-plaintext highlighter-rouge">log_event</code>, <code class="language-plaintext highlighter-rouge">log_data_equip</code>, <code class="language-plaintext highlighter-rouge">log_data_trans</code>, <code class="language-plaintext highlighter-rouge">log_err_equip</code>, <code class="language-plaintext highlighter-rouge">log_err_trans</code></td>
    </tr>
  </tbody>
</table>

<hr />

<p><br /></p>

<h2 id="3-smartcast-robotics-project--erd--pydantic-클래스명-매핑">3. SmartCast Robotics Project — ERD → Pydantic 클래스명 매핑</h2>

<details><summary>Master tables → [테이블명]Record</summary>
<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>DB 테이블</th>
      <th>Pydantic 클래스</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">user_account</code></td>
      <td><code class="language-plaintext highlighter-rouge">UserAccountRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">category</code></td>
      <td><code class="language-plaintext highlighter-rouge">CategoryRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">product</code></td>
      <td><code class="language-plaintext highlighter-rouge">ProductRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">product_option</code></td>
      <td><code class="language-plaintext highlighter-rouge">ProductOptionRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pp_options</code></td>
      <td><code class="language-plaintext highlighter-rouge">PpOptionsRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">res</code></td>
      <td><code class="language-plaintext highlighter-rouge">ResRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">equip_load_spec</code></td>
      <td><code class="language-plaintext highlighter-rouge">EquipLoadSpecRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">zone</code></td>
      <td><code class="language-plaintext highlighter-rouge">ZoneRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">equip</code></td>
      <td><code class="language-plaintext highlighter-rouge">EquipRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">trans</code></td>
      <td><code class="language-plaintext highlighter-rouge">TransRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">trans_task_bat_threshold</code></td>
      <td><code class="language-plaintext highlighter-rouge">TransTaskBatThresholdRecord</code></td>
    </tr>
  </tbody>
</table>

</details>

<details><summary>Operational tables → Input / Record</summary>
<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>DB 테이블</th>
      <th>쓰임</th>
      <th>Pydantic 클래스</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord</code></td>
      <td>주문 생성 입력</td>
      <td><code class="language-plaintext highlighter-rouge">CreateOrdInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">OrdRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_detail</code></td>
      <td>상세 생성 입력</td>
      <td><code class="language-plaintext highlighter-rouge">CreateOrdDetailInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_detail</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">OrdDetailRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_pp_map</code></td>
      <td>후처리 옵션 매핑 입력</td>
      <td><code class="language-plaintext highlighter-rouge">CreateOrdPpMapInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_pp_map</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">OrdPpMapRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_txn</code></td>
      <td>주문 상태 전이 입력</td>
      <td><code class="language-plaintext highlighter-rouge">CreateOrdTxnInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_txn</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">OrdTxnRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">equip_task_txn</code></td>
      <td>태스크 할당 입력</td>
      <td><code class="language-plaintext highlighter-rouge">AssignEquipTaskInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">equip_task_txn</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">EquipTaskTxnRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">trans_task_txn</code></td>
      <td>태스크 할당 입력</td>
      <td><code class="language-plaintext highlighter-rouge">AssignTransTaskInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">trans_task_txn</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">TransTaskTxnRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">insp_task_txn</code></td>
      <td>검사 태스크 생성 입력</td>
      <td><code class="language-plaintext highlighter-rouge">CreateInspTaskInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">insp_task_txn</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">InspTaskTxnRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pp_task_txn</code></td>
      <td>후처리 태스크 생성 입력</td>
      <td><code class="language-plaintext highlighter-rouge">CreatePpTaskInput</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pp_task_txn</code></td>
      <td>DB 행 조회</td>
      <td><code class="language-plaintext highlighter-rouge">PpTaskTxnRecord</code></td>
    </tr>
  </tbody>
</table>

</details>

<details><summary>State tables → [테이블명]Record</summary>
<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>DB 테이블</th>
      <th>Pydantic 클래스</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">item_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">ItemStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">OrdStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">equip_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">EquipStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">trans_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">TransStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">chg_loc_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">ChgLocStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">strg_loc_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">StrgLocStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ship_loc_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">ShipLocStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pattern_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">PatternStatRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">alerts_stat</code></td>
      <td><code class="language-plaintext highlighter-rouge">AlertsStatRecord</code></td>
    </tr>
  </tbody>
</table>

</details>

<details><summary>Log tables → Event / Record</summary>
<table class="table table-sm table-striped">
  <thead>
    <tr>
      <th>DB 테이블</th>
      <th>Pydantic 클래스</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ord_log</code></td>
      <td><code class="language-plaintext highlighter-rouge">OrdLogRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_action_user</code></td>
      <td><code class="language-plaintext highlighter-rouge">UserActionEvent</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_action_operator_handoff_acks</code></td>
      <td><code class="language-plaintext highlighter-rouge">OperatorHandoffEvent</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_action_operator_rfid_scan</code></td>
      <td><code class="language-plaintext highlighter-rouge">RfidScanEvent</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_action_admin</code></td>
      <td><code class="language-plaintext highlighter-rouge">AdminActionEvent</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_event</code></td>
      <td><code class="language-plaintext highlighter-rouge">SystemEvent</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_data_equip</code></td>
      <td><code class="language-plaintext highlighter-rouge">EquipDataRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_data_trans</code></td>
      <td><code class="language-plaintext highlighter-rouge">TransDataRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_err_equip</code></td>
      <td><code class="language-plaintext highlighter-rouge">EquipErrorRecord</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">log_err_trans</code></td>
      <td><code class="language-plaintext highlighter-rouge">TransErrorRecord</code></td>
    </tr>
  </tbody>
</table>

</details>

<h3 id="함수-흐름-예시">함수 흐름 예시</h3>

<p>Pydantic 클래스 네이밍이 왜 중요한지는 실제 함수 흐름을 보면 이해가 된다.</p>

<p>각 함수는 <strong>Pydantic 클래스를 입력으로 받고, Pydantic 클래스를 반환</strong>한다. 함수 시그니처만 봐도 어떤 데이터가 들어오고 나가는지, 어떤 목적의 클래스인지 바로 알 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">create_ord</span><span class="p">(</span><span class="n">data</span><span class="p">:</span> <span class="n">CreateOrdInput</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">OrdRecord</span><span class="p">:</span> <span class="bp">...</span>
<span class="c1">#                    ↑ Input suffix    ↑ Record suffix
#              "주문 생성 입력값"       "DB에서 꺼낸 주문 행"
</span>
<span class="k">def</span> <span class="nf">assign_equip_task</span><span class="p">(</span><span class="n">data</span><span class="p">:</span> <span class="n">AssignEquipTaskInput</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">EquipTaskTxnRecord</span><span class="p">:</span> <span class="bp">...</span>
<span class="k">def</span> <span class="nf">get_trans_stat</span><span class="p">(</span><span class="n">trans_id</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">TransStatRecord</span><span class="p">:</span> <span class="bp">...</span>
<span class="k">def</span> <span class="nf">log_trans_error</span><span class="p">(</span><span class="n">event</span><span class="p">:</span> <span class="n">TransErrorEvent</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span> <span class="bp">...</span>
</code></pre></div></div>

<p>이 패턴의 핵심은 <strong>검증이 경계마다 자동으로 일어난다</strong>는 것이다. 외부에서 잘못된 값이 들어오면 <code class="language-plaintext highlighter-rouge">CreateOrdInput</code> 생성 시점에 즉시 <code class="language-plaintext highlighter-rouge">ValidationError</code>가 나고, DB 조회 결과를 <code class="language-plaintext highlighter-rouge">OrdRecord</code>로 변환할 때도 타입이 맞지 않으면 바로 에러가 난다. 값이 함수 안 깊숙이 들어가서 터지는 게 아니라 <strong>경계에서 먼저 막힌다</strong>.</p>

<pre><code class="language-mermaid">flowchart TD
    A[외부 입력] --&gt; B["CreateOrdInput\nPydantic 타입·필드 자동 검증"]
    B --&gt; C["create_ord()\nDB INSERT"]
    C --&gt; D["OrdRecord\nDB 행 반환"]
    D --&gt; E["assign_equip_task()\nAssignEquipTaskInput 생성 후 DB INSERT"]
    E --&gt; F["EquipTaskTxnRecord\n완료된 태스크 행 반환"]
    F --&gt;|에러 발생 시| G[TransErrorEvent]
    G --&gt; H["log_trans_error()"]
    H --&gt; I[log_err_trans 저장]
</code></pre>

<p><code class="language-plaintext highlighter-rouge">OrdRecord</code>처럼 DB 조회 결과를 받는 클래스에는 앞서 설명한 <code class="language-plaintext highlighter-rouge">from_attributes=True</code>가 붙어서, SQLAlchemy ORM 객체를 바로 Pydantic 모델로 변환할 수 있다.</p>

<hr />

<h3 id="제네릭-db-유틸-함수-패턴--typet">제네릭 DB 유틸 함수 패턴 — <code class="language-plaintext highlighter-rouge">type[T]</code></h3>

<p>위 함수들은 테이블마다 별도로 만들지 않고, <strong>어떤 Pydantic 모델이든 받을 수 있는 제네릭 함수</strong> 하나로 공통화할 수 있다. <code class="language-plaintext highlighter-rouge">state_manager.py</code>가 이 패턴을 사용한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pydantic</span> <span class="kn">import</span> <span class="n">BaseModel</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">TypeVar</span>

<span class="n">T</span> <span class="o">=</span> <span class="nc">TypeVar</span><span class="p">(</span><span class="sh">"</span><span class="s">T</span><span class="sh">"</span><span class="p">,</span> <span class="n">bound</span><span class="o">=</span><span class="n">BaseModel</span><span class="p">)</span>
<span class="c1"># "T는 아직 정해지지 않은 타입인데, BaseModel을 상속한 클래스여야 한다"
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">TypeVar</code>는 함수가 호출될 때 구체적인 타입으로 확정되는 자리표시자다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">insert_row</span><span class="p">(</span>
    <span class="n">pool</span><span class="p">:</span> <span class="n">asyncpg</span><span class="p">.</span><span class="n">Pool</span><span class="p">,</span>
    <span class="n">table</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">data</span><span class="p">:</span> <span class="n">BaseModel</span><span class="p">,</span>       <span class="c1"># 어떤 Pydantic 인스턴스든 받음
</span>    <span class="n">return_type</span><span class="p">:</span> <span class="nb">type</span><span class="p">[</span><span class="n">T</span><span class="p">],</span>  <span class="c1"># 어떤 Pydantic 클래스 자체를 받음
</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">T</span><span class="p">:</span>                    <span class="c1"># 반환 타입은 넘긴 클래스와 동일하게 확정
</span>    <span class="n">row</span> <span class="o">=</span> <span class="k">await</span> <span class="n">c</span><span class="p">.</span><span class="nf">fetchrow</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="o">*</span><span class="n">vals</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">return_type</span><span class="p">(</span><span class="o">**</span><span class="nf">dict</span><span class="p">(</span><span class="n">row</span><span class="p">))</span>   <span class="c1"># 넘긴 클래스로 인스턴스 생성
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">type[T]</code>는 인스턴스가 아니라 <strong>클래스 자체</strong>를 받는다는 뜻이다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">OrdRecord</span><span class="p">(</span><span class="n">ord_id</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="p">...)</span>  <span class="c1"># 인스턴스  → data: BaseModel 에 해당
</span><span class="n">OrdRecord</span>                 <span class="c1"># 클래스 자체 → return_type: type[T] 에 해당
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">T</code>가 <code class="language-plaintext highlighter-rouge">return_type</code>과 반환값을 <strong>묶어주는 역할</strong>을 하기 때문에, 어떤 클래스를 넘기냐에 따라 반환 타입이 자동으로 추론된다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># OrdRecord를 넘기면 → T = OrdRecord → 반환값이 OrdRecord로 추론됨
</span><span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">insert_row</span><span class="p">(</span><span class="n">pool</span><span class="p">,</span> <span class="sh">"</span><span class="s">ord</span><span class="sh">"</span><span class="p">,</span> <span class="n">data</span><span class="p">,</span> <span class="n">OrdRecord</span><span class="p">)</span>
<span class="n">result</span><span class="p">.</span><span class="n">ord_id</span>   <span class="c1"># 타입 체커(mypy/pyright)가 자동완성 인식
</span>
<span class="c1"># EquipTaskTxnRecord를 넘기면 → T = EquipTaskTxnRecord
</span><span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">insert_row</span><span class="p">(</span><span class="n">pool</span><span class="p">,</span> <span class="sh">"</span><span class="s">equip_task_txn</span><span class="sh">"</span><span class="p">,</span> <span class="n">data</span><span class="p">,</span> <span class="n">EquipTaskTxnRecord</span><span class="p">)</span>
<span class="n">result</span><span class="p">.</span><span class="n">txn_stat</span>  <span class="c1"># 마찬가지로 인식
</span></code></pre></div></div>

<p>이 패턴 덕분에 테이블마다 함수를 만들지 않고 <code class="language-plaintext highlighter-rouge">insert_row</code> / <code class="language-plaintext highlighter-rouge">read_stat</code> / <code class="language-plaintext highlighter-rouge">update_stat</code> 세 개로 모든 테이블을 처리할 수 있다.</p>]]></content><author><name></name></author><category term="guide" /><category term="team" /><category term="pydantic" /><category term="pep8" /><summary type="html"><![CDATA[Class naming protocols for collaborating on code]]></summary></entry><entry><title type="html">Qwen3-VL</title><link href="https://daye-lee18.github.io/blog/2025/Qwen3-VL/" rel="alternate" type="text/html" title="Qwen3-VL" /><published>2025-12-11T00:00:00+00:00</published><updated>2025-12-11T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2025/Qwen3-VL</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2025/Qwen3-VL/"><![CDATA[<h2 id="equations">Equations</h2>]]></content><author><name>Daye Lee</name></author><category term="formatting" /><summary type="html"><![CDATA[Multimodal LLM by Alibaba Qwen team]]></summary></entry><entry><title type="html">Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra</title><link href="https://daye-lee18.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/" rel="alternate" type="text/html" title="Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra" /><published>2024-05-14T00:00:00+00:00</published><updated>2024-05-14T00:00:00+00:00</updated><id>https://daye-lee18.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/"><![CDATA[<p>Learn more:Learn more:Learn more:Learn more:Learn more:Learn more:May 14, 2024
          We’re introducing a series of updates across the Gemini family of models, including the new 1.5 Flash, our lightweight model for speed and efficiency, and Project Astra, our vision for the future of AI assistants.
        In December, we launched our first natively multimodal model Gemini 1.0 in three sizes: Ultra, Pro and Nano. Just a few months later we released 1.5 Pro, with enhanced performance and a breakthrough long context window of 1 million tokens.Developers and enterprise customers have been putting 1.5 Pro to use in incredible ways and finding its long context window, multimodal reasoning capabilities and impressive overall performance incredibly useful.We know from user feedback that some applications need lower latency and a lower cost to serve. This inspired us to keep innovating, so today, we’re introducing Gemini 1.5 Flash: a model that’s lighter-weight than 1.5 Pro, and designed to be fast and efficient to serve at scale.Both 1.5 Pro and 1.5 Flash are available in public preview with a 1 million token context window in Google AI Studio and Vertex AI. And now, 1.5 Pro is also available with a 2 million token context window via waitlist to developers using the API and to Google Cloud customers.We’re also introducing updates across the Gemini family of models, announcing our next generation of open models, Gemma 2, and sharing progress on the future of AI assistants, with Project Astra.Context lengths of leading foundation models compared with Gemini 1.5’s 2 million token capability1.5 Flash is the newest addition to the Gemini model family and the fastest Gemini model served in the API. It’s optimized for high-volume, high-frequency tasks at scale, is more cost-efficient to serve and features our breakthrough long context window.While it’s a lighter weight model than 1.5 Pro, it’s highly capable of multimodal reasoning across vast amounts of information and delivers impressive quality for its size.The new Gemini 1.5 Flash model is optimized for speed and efficiency, is highly capable of multimodal reasoning and features our breakthrough long context window.1.5 Flash excels at summarization, chat applications, image and video captioning, data extraction from long documents and tables, and more. This is because it’s been trained by 1.5 Pro through a process called “distillation,” where the most essential knowledge and skills from a larger model are transferred to a smaller, more efficient model.Read more about 1.5 Flash in our updated Gemini 1.5 technical report, on the Gemini technology page, and learn about 1.5 Flash’s availability and pricing.Over the last few months, we’ve significantly improved 1.5 Pro, our best model for general performance across a wide range of tasks.Beyond extending its context window to 2 million tokens, we’ve enhanced its code generation, logical reasoning and planning, multi-turn conversation, and audio and image understanding through data and algorithmic advances. We see strong improvements on public and internal benchmarks for each of these tasks.1.5 Pro can now follow increasingly complex and nuanced instructions, including ones that specify product-level behavior involving role, format and style. We’ve improved control over the model’s responses for specific use cases, like crafting the persona and response style of a chat agent or automating workflows through multiple function calls. And we’ve enabled users to steer model behavior by setting system instructions.We added audio understanding in the Gemini API and Google AI Studio, so 1.5 Pro can now reason across image and audio for videos uploaded in Google AI Studio. And we’re now integrating 1.5 Pro into Google products, including Gemini Advanced and in Workspace apps.Read more about 1.5 Pro in our updated Gemini 1.5 technical report and on the Gemini technology page.Gemini Nano is expanding beyond text-only inputs to include images as well. Starting with Pixel, applications using Gemini Nano with Multimodality will be able to understand the world the way people do — not just through text, but also through sight, sound and spoken language.Read more about Gemini 1.0 Nano on Android.Today, we’re also sharing a series of updates to Gemma, our family of open models built from the same research and technology used to create the Gemini models.We’re announcing Gemma 2, our next generation of open models for responsible AI innovation. Gemma 2 has a new architecture designed for breakthrough performance and efficiency, and will be available in new sizes.The Gemma family is also expanding with PaliGemma, our first vision-language model inspired by PaLI-3. And we’ve upgraded our Responsible Generative AI Toolkit with LLM Comparator for evaluating the quality of model responses.Read more on the Developer blog.As part of Google DeepMind’s mission to build AI responsibly to benefit humanity, we’ve always wanted to develop universal AI agents that can be helpful in everyday life. That’s why today, we’re sharing our progress in building the future of AI assistants with Project Astra (advanced seeing and talking responsive agent).To be truly useful, an agent needs to understand and respond to the complex and dynamic world just like people do — and take in and remember what it sees and hears to understand context and take action. It also needs to be proactive, teachable and personal, so users can talk to it naturally and without lag or delay.While we’ve made incredible progress developing AI systems that can understand multimodal information, getting response time down to something conversational is a difficult engineering challenge. Over the past few years, we’ve been working to improve how our models perceive, reason and converse to make the pace and quality of interaction feel more natural.Building on Gemini, we’ve developed prototype agents that can process information faster by continuously encoding video frames, combining the video and speech input into a timeline of events, and caching this information for efficient recall.By leveraging our leading speech models, we also enhanced how they sound, giving the agents a wider range of intonations. These agents can better understand the context they’re being used in, and respond quickly, in conversation.With technology like this, it’s easy to envision a future where people could have an expert AI assistant by their side, through a phone or glasses. And some of these capabilities are coming to Google products, like the Gemini app and web experience, later this year.We’ve made incredible progress so far with our family of Gemini models, and we’re always striving to advance the state-of-the-art even further. By investing in a relentless production line of innovation, we’re able to explore new ideas at the frontier, while also unlocking the possibility of new and exciting Gemini use cases.Learn more about Gemini and its capabilities.
            Your information will be used in accordance with
            Google’s privacy policy.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>      Done. Just one step more.
    
      Check your inbox to confirm your subscription.
    You are already subscribed to our newsletter.
    You can also subscribe with a
    different email address
    
    .
    
  Let’s stay in touch. Get the latest news from Google in your inbox.
          Follow Us
</code></pre></div></div>]]></content><author><name></name></author><summary type="html"><![CDATA[We’re sharing updates across our Gemini family of models and a glimpse of Project Astra, our vision for the future of AI assistants.]]></summary></entry><entry><title type="html">Displaying External Posts on Your al-folio Blog</title><link href="https://daye-lee18.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/" rel="alternate" type="text/html" title="Displaying External Posts on Your al-folio Blog" /><published>2022-04-23T23:20:09+00:00</published><updated>2022-04-23T23:20:09+00:00</updated><id>https://daye-lee18.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog</id><content type="html" xml:base="https://daye-lee18.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/"><![CDATA[<h3>External Posts on Your al-folio Blog</h3>
<p>If you prefer publishing blog posts on medium.com or other external sources, starting version v0.5.0, <a href="https://github.com/alshedivat/al-folio">al-folio</a> lets you to display your external posts in the blog feed of your website! 🎉🎉</p>
<p>Configuring external sources of super simple. After upgrading to v0.5.0, just add the following section to your _config.yml:</p>
<pre>external_sources:<br />  - name: medium.com  # name of the source (arbitrary string)<br />    rss_url: <a href="https://medium.com/@al-folio/feed">https://medium.com/@&lt;your-medium-username&gt;/feed</a></pre>
<p>The example above adds your medium.com blog post feed as an external source. But you can add arbitrary RSS feeds as sources.</p>
<p>Any questions or suggestions? 👉 Start <a href="https://github.com/alshedivat/al-folio/discussions">a discussion on GitHub</a>!</p>
<p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=b60a1d241a0a" width="1" height="1" alt="" /></p>]]></content><author><name></name></author></entry></feed>